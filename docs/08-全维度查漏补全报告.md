# 08 · 全维度查漏补全报告（2026-09-07）

> 用途：用户要求"详细梳理整个项目流程，看架构/技术/细节/UI/部署/内容有无遗漏，尽快找出漏洞并补全"。本文是这次系统复查的产出：**A 已补全项**（本轮直接落盘或新增设计）+ **B 仍存漏洞/缺口清单**（按维度列出，给处理建议与责任方）+ **C 待用户拍板的真实缺口**。前期方案阶段，不写代码。

## 0. TL;DR

经全维度复查，**架构主体成立、无阻断性设计漏洞**；2000 万字容量升级（02 §18）与类目重组（06 §7）已落盘。本轮新发现 **12 项缺口**，其中 **4 项需在 M0 前补全/定案**（标 ★），其余为实施期或可延后项。最关键的两项是：① 2000 万字下"全库索引仍随包走"与"导入永不重建"在超大库时索引体积（压缩 50MB）对手机导入体验的二次压力——需补"学科索引包"粒度规范；② "汇编体著作（卷为无正文容器）"的 Section 树建模口径未定，影响毛选/邓选等整本录入。

---

## A. 本轮已补全项（直接落盘）

| # | 维度 | 补全内容 | 落点 |
|---|------|----------|------|
| A1 | 架构 | 2000 万字容量升级裁决：分片 16→256、Worker 分级常驻+LRU+IndexedDB 缓存、不引 SQLite、三层内容分发、性能预算表全更新 | 02 §18 |
| A2 | 内容/架构 | 类目重组 v2：经济学升 L1(含金融 L2)、心理学并入科学、政治理论"中国政治理论"扩容(领袖选集+党会议文献)、taxonomy 骨架、categories[] 迁移方案 | 06 §7 |
| A3 | UI/性能 | 05 §4 冷启动链修订（首屏与全文索引彻底解耦）；05 §2.4 选区工具条；05 §6 Q22–Q25 固化 | 05 |
| A4 | 数据可信 | 04 §2.4.5 版权三档、§2.5 密度轴两级提炼、§2.6 标记问题回传 feedback.json | 04 |
| A5 | 一致性 | 01b 顶部 v2 supersede 横幅；01 §8.1 分片包结构重写、§9.1 _browse 影子树；03 §6 五项解决、§7 Q15 定案 | 01b/01/03 |

---

## B. 仍存漏洞 / 缺口清单（按维度）

### B-架构

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B1 | **★ 汇编体著作 Section 树建模口径未定** | 毛选/邓选/江选等"卷为无正文容器、正文在篇"的汇编体，02 现有 Section 树（卷→篇→节）假定每层有正文；卷层无正文时 Lint/渲染/续读如何处理未定 | 定：`type=work` 的 `section.kind` 增 `container`（卷，无正文，仅作分组）；Lint 允许 container 节点无 body；续读锚点跳过 container 指向最近子篇；TOC 显示 container 为折叠标题 | 架构师 | M0 前 |
| B2 | **★ 全库索引随包走 vs 学科索引包粒度** | 02 §18.4 说"包必带索引分区"，但全库索引压缩 ~50MB，单学科包索引压缩 ≤10MB——手机导入一个全库包 50MB 索引体验差；是否应有"学科索引包"独立分发未定 | 定：包 `manifest.pack.level` 区分 `seed/digest/full`；full 包带全库索引分区(50MB 压缩)，digest 包只带该学科摘要索引分区，full 学科包带该学科正文索引分区；导入按 `index_shards` 只覆盖对应片 | 架构师 | M0 前 |
| B3 | 索引分区数 M 在"中等规模(300–1000 万字)"下用 64 还是直接 256 未给档位切换阈值 | 过早上 256 致单片过细；过晚上 16 致单片过大 | 给阈值表：≤300万 M=16；300–1000万 M=64；≥1000万 M=256；`builder` 按词条数自动选档或常量切换 | 架构师 | 实施期 |
| B4 | `cross_timeline` 字段在 Track schema 中未正式定义 | 历史对照视图连接线数据来源（06 §7.3 依赖它）未落 TrackItem schema | 01b §6.2 TrackItem 增 `cross_timeline?: boolean`（标记该词条在双轨同时出现）；02 索引 doc 表含 timeline/sort_date/cross_timeline 供渲染读取 | 架构师/产品 | M0 前 |

### B-技术

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B5 | 分词词典来源与许可未定（02 §17 项8 仍挂） | bigram 兜底可用，但准确率；M1 检索实测需词典 | M1 选定：jieba 词典 BSD-3（或同等许可的开源中文词表）；词典为可选增强，缺失降级 bigram 不破坏同构 | 架构师 | M1 |
| B6 | slug 中译英映射表维护机制未工程化 | 02 §17 项9 说"随内容积累维护"，但无 CLI 检测/提示 | `pks lint` 增 L0xx：slug 命中映射表则用映射、否则拼音兜底并标 `slug_pinyin` 标记供人工复核；映射表 `slug-map.yaml` 纳入 Git | 架构师 | 实施期 |

### B-细节

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B7 | 章节级 `section.summary` 在 Seed 内是否截断与 L-D 包对齐口径 | 02 §18.7 说 Seed 内 summary 截断 ~120–150 字、完整版放 L-D，但 04 §2.5 的 `section.summary` schema 是单字段——截断/完整如何分版未定 | 定：`section.summary` 内分 `{tldr(≤120), keyPoints[]}`，Seed 只存 tldr；完整 keyPoints 随 entries 分片/L-D 包；渲染时若 keyPoints 缺则显示 tldr+"导入摘要包查看要点" | 架构师 | M0 前 |
| B8 | "导入全文包后已读摘要标记/进度是否保留"未明 | 用户先读摘要再导全文，续读位置与已读状态是否跨包保留 | userdata.json 的阅读位置/已读标记按 slug+sectionId 键，与正文包级别无关；导入全文包后自动接续 | 架构师/产品 | 实施期 |

### B-UI

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B9 | 选区工具条与"滚动隐藏底栏"在选区时的 z-index/焦点冲突未定 | 05 §2.4 说"不冲突"但未给层级规范 | 定：选区工具条 `z-index` 高于底栏；选区激活时底栏强制保持隐藏态；工具条消失后才允许底栏手势唤回 | 架构师/UI | 实施期 |
| B10 | 历史对照视图"时间带分组"在手机端密度自适应算法未定 | 06 §7.3 说"每 25/50/100 年随密度自适应"但无算法 | 给：按可视区高度与该带条目数动态选档（≤6 条/带→细档；>12→粗档），上限避免溢出 | 前端 | 实施期 |

### B-部署

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B11 | **★ 局域网 Wi-Fi 传输(X-05)在 2000 万字档是否仍可行** | 全库 zip ~120MB，手机经浏览器下载 + App 打开的链路是否稳；serve-bundle 临时服务在大文件下的断点续传未定 | 定：serve-bundle 支持分学科包分次传（不一次 120MB）；浏览器下载走流式 + 进度；超 25MB 建议改"电脑导出文件→手动拷贝到手机存储→App 内导入"路径 | 架构师 | M0 前 |

### B-内容

| # | 漏洞/缺口 | 影响 | 处理建议 | 责任 | 紧急度 |
|---|----------|------|----------|------|--------|
| B12 | 受保护篇目"尽量全文"的判定标准模糊 | 决策 #10 说"摘要+导读为主、尽量全文"，但何谓"尽量"无量化 | 定操作边界：受保护篇目先抓"权威公开发布全文文本"（新华社/人民出版社电子版/官方数据库）→ 有则按 T1/T2 收录全文+标源；无权威全文则摘要+导读+≤10%权威引文片段；**每批开始前由用户确认范围**（接 04 §2.4.4） | 助手/产品 | 录入期 |

---

## C. 待用户拍板的真实缺口

| # | 问题 | 选项 | 影响 |
|---|------|------|------|
| C1 | apk 体积硬约束 ≤45MB（Seed 全量）还是 ≤30MB（Seed 再砍章节级摘要）？ | A=≤45MB（装完即读到六类摘要+精选全文，推荐）/ B=≤30MB（章节级摘要全进 L-D 包） | 02 §18.4 P18；装完即得 vs 更轻 |
| C2 | 金融是否在首页/底栏保留"金融"直达快捷入口（非 taxonomy 第二根）？ | A=保留（推荐，原读者心智）/ B=不保留，统一从经济学进 | 06 §7.9 开放问题2 |
| C3 | 领袖著作建模口径：一部"选"= 单 work 词条+卷→篇→节（产品默认）vs 每篇独立词条+选集综述？ | A=单 work+container Section（推荐，复用《资本论》管线）/ B=每篇独立 | B1；影响毛选/邓选等录入 |
| C4 | 文学/法律/艺术 B2 首批扩展是否纳入？何时？ | A=B2 起逐步（推荐）/ B=暂不 | 06 §7.8；不阻塞 M0 |

> C1–C3 建议在冻结 M0 前定（影响索引/包体/著作建模）；C4 可 B2 再定。

---

## D. 建议的下一步

1. 我先就 **B1/B2/B4/B7/B11**（标 ★ 的架构/部署项）补出正式设计裁决（可再调度架构师一轮），连同 **C1–C3** 一并请你拍板。
2. 拍板后即可**冻结 M0**，进入 T01（M0 最小子集已划好，02 §15.3，≈14 人日；2000 万字档下需在 T02mini 补"分区懒加载 + IndexedDB 缓存"骨架）。
3. 本报告作为查漏总账；后续若你补充需求，我据此增量登记。

---

*复查人：主理人 齐活林（Qi） · 交付总监*
*日期：2026-09-07*

---

## 更新（2026-09-07 · 五项 ★ 缺口已闭环）

用户拍板 C1–C4（apk ≤80MB / 金融不保留直达入口 / 选集=单 work+卷→篇→节 / 文学法律艺术暂不纳入）后，架构师高见远补全 B1/B2/B4/B7/B11 五项设计裁决，已落盘至 **`02 §18.9–§18.14`**：

| 缺口 | 裁决一句话 | 落点 |
|---|---|---|
| **B1** ★ | Section 增 `kind:'content'\|'container'`，卷=container 无正文不计字数、续读下钻、TOC 折叠；复用《资本论》管线承载领袖选集 | 02 §18.9 |
| **B2** ★ | apk ≤80MB 下 Seed ~52–60MB（全 L1+全部 summary+全部 tldr+精选 T1 全文+Seed 索引）；tldr 25MB raw 上限 + 8M字降级阈值；`pack.level=seed\|digest\|full` 三级分发 | 02 §18.13 |
| **B4** ★ | TrackItem 增 `cross_timeline?: boolean`（取代旧 string）；EntryIndexItem/SearchDoc 增 `tl/sd/ctl`；build 期按 entry slug 跨轨匹配推导 | 02 §18.11 |
| **B7** ★ | `section.summary={tldr≤120, keyPoints?[]}`；Seed 只存 tldr，keyPoints 随 L-D 包；缺失降级显示 tldr + "导入摘要包"按钮 | 02 §18.12 |
| **B11** ★ | serve-bundle 按学科包分次传；≤25MB 走 HTTP Range+分片 sha256 断点续传；25–80MB 建议手动拷贝；>80MB 强制手动或按卷拆包 | 02 §18.10 |

> 五项 ★ 全部闭环，跨项一致性自检通过（02 §18.14）。08 报告中 B3/B5/B6/B8/B9/B10/B12 为实施期/录入期项，不阻塞 M0。**冻结 M0 的技术前提已具备。**

### C1–C4 决策登记（已写入 03 #14–#17）

| # | 决策 | 结论 |
|---|---|---|
| 14 | apk 体积上限 | **≤80MB** |
| 15 | 金融直达入口 | **不保留** |
| 16 | 领袖选集建模 | 单 work 词条 + 卷→篇→节（复用《资本论》管线） |
| 17 | 文学/法律/艺术扩展 | 暂不纳入一期，保持 B2 候选建议 |
