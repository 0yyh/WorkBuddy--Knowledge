# PKS 个人知识学习站 · 项目结构整理与现状评估

> 生成时间：2026-09-10 · 本轮为**目录整理 + 现状盘点**（含安全清理），未改动任何业务逻辑。
> 清理后体积 **296 MB → 173 MB**（≈ −123 MB）；工具链复验：`typecheck` ✅ / `vitest 135 通过` ✅ / `lint 0 error 0 warn` ✅。

---

## 〇、本次清理总结

### 清理依据
以项目自身的 `.gitignore` 为权威口径——凡被其声明为“非源码/派生物”的目录，即为可清理对象；
同时逐项确认**不会破坏运行与构建**。

### 已删除（全部可再生，或纯垃圾）

| 类别 | 具体项 | 体积 |
| --- | --- | --- |
| 临时日志 | `.dict.log`、`.tcdict.log`、`build-apk-home-v5.log`、`release-build.log`、`apps/web/tsc-check.log` | ~10 KB |
| 历史回收站 | `.trash-2026-09-09/`（16 个旧构建日志 + `gradle-8.11.1-all.zip` + 1.jpg） | 197 KB |
| 测试临时产物 | `packages/core/vitest.config.ts.timestamp-*.mjs` × 5 | 20 KB |
| 内容派生 | `content/.index/`（15 MB）、`content/_browse/`、`content/_bundles/` | 15 MB |
| Web 构建产物 | `apps/web/dist/`（19 MB）、`apps/web/public/content/`（18 MB） | 37 MB |
| Android 构建缓存 | `apps/web/android/.gradle/`、`android/build/`、`android/app/build/` | 56 MB |
| OTA 数据包 | `release/latest/`（可用 `build:update` 秒级重建） | 18 MB |

### 明确保留（必要文件 / 核心代码 / 交付物）

- **全部源码**：`packages/core/src`、`packages/cli/src`、`apps/web/src`（共 79 个源文件）。
- **全部内容**：`content/entries`（101 词条）、`content/tracks`（5 轨）、`content/dict`、`content/taxonomy.yaml`。
- **全部配置**：`package.json`、`tsconfig.base.json`、`vite.config.ts`、`capacitor.config.ts`、`.gitignore`、`.editorconfig`、`.prettierrc`。
- **手改 Gradle 配置（关键，不可用 `cap sync` 重建）**：`android/build.gradle`、`variables.gradle`、`gradle.properties`、`app/build.gradle`、`gradlew*`、`gradle/` 包装器。
- **`packages/{core,cli}/dist`**：体积小（~580 KB）且被 cli/web 以 `@pks/core` 引用，保留以维持即用性。
- **`release/pks-app-2026-09-10.apk`**（7.35 MB）：唯一交付二进制，保留。
- **`docs/`（13 份设计文档）、`.workbuddy/`（记忆与技能数据，禁止删除）、`node_modules/`（依赖）。**

### 清理后如何恢复可运行态（按需执行）

```bash
npm run build:index      # content/ → content/.index/（重建检索索引）
npm run content:copy     # content/ → apps/web/public/content/（.index 改名 index）
npm run build            # 重建 web 产物 apps/web/dist/
# 出 APK（会依次 cap sync → gradle assembleDebug）：
npm run android:build    # 产物：apps/web/android/app/build/outputs/apk/debug/app-debug.apk
# 生成 App 内 OTA 内容包：
npm run build:update     # → release/latest/
```

---

## 一、当前完整目录结构

```
D:\WorkBuddy--Knowledge\
│
├─ apps/
│  └─ web/                            # 阅读端：Web 与 Android 共用同一套代码
│     ├─ src/
│     │  ├─ pages/                    # 9 个页面
│     │  │   Home.tsx  BrowsePage.tsx  EntryCoverPage.tsx  EntryReaderPage.tsx
│     │  │   SearchPage.tsx  TimelinePage.tsx  HistoryPage.tsx  SettingsPage.tsx  MePage.tsx
│     │  ├─ components/               # 10 个组件
│     │  │   AppShell.tsx  TabBar.tsx  CategoryTree.tsx  SearchBox.tsx  SettingControls.tsx
│     │  │   TimelineView.tsx  ReaderSettingsSheet.tsx  ReaderSelectionMenu.tsx
│     │  │   ConfirmDialog.tsx  ExitToast.tsx  Spinner.tsx
│     │  ├─ lib/                      # 11 个能力模块
│     │  │   loader.ts  content.ts  contentCache.ts  contentUpdater.ts  cover.ts
│     │  │   dict.ts  history.ts  preferences.ts  relativeTime.ts  backHandler.ts  update.ts
│     │  ├─ state/                    # AppContext.tsx  ImmersiveContext.tsx
│     │  ├─ App.tsx  main.tsx  router.tsx  types.ts  styles.css
│     ├─ public/                      # favicon 等静态图标（content/ 为派生物，已清理）
│     ├─ android/                     # Capacitor 7 Android 工程（内含手改 Gradle 配置）
│     │  ├─ app/
│     │  │  ├─ build.gradle  src/main/{java/com/pks, AndroidManifest.xml, assets/, res/}
│     │  │  └─ src/{test,androidTest}/
│     │  ├─ build.gradle  variables.gradle  gradle.properties  settings.gradle
│     │  ├─ capacitor.settings.gradle  gradlew  gradlew.bat  gradle/  local.properties
│     │  └─ capacitor-cordova-android-plugins/
│     ├─ scripts/
│     ├─ capacitor.config.ts  index.html  package.json  tsconfig.json
│     ├─ vite.config.ts  package-lock.json
│
├─ packages/
│  ├─ core/                           # @pks/core · 同构核心（无 DOM / 主入口无 node:*）
│  │  ├─ src/                         # 32 文件：parse / vfs / index / dict / track / merge / util / content
│  │  ├─ test/                        # 20 个测试文件 / 135 用例（vitest）
│  │  ├─ dist/                        # 构建产物（被 cli/web 以 @pks/core 引用）
│  │  └─ package.json  tsconfig.json  vitest.config.ts
│  └─ cli/                            # @pks/cli · 内容工具链
│     ├─ src/                         # 8 文件
│     │  ├─ index.ts                  # 命令分发（build:index/lint/bundle/rename/browse:gen/search）
│     │  ├─ load-index.ts
│     │  └─ commands/                 # build-index.ts lint.ts bundle.ts rename.ts browse-gen.ts search.ts
│     └─ dist/  package.json  tsconfig.json
│
├─ content/                           # 内容仓库（唯一事实来源）
│  ├─ entries/                        # 101 个词条目录（entry.md + chapters/*.md，共 601 个 md）
│  ├─ tracks/                         # 5 条学习轨迹 *.yaml
│  ├─ dict/dictionary.json            # 离线词典（划词查询）
│  ├─ taxonomy.yaml                   # 类目树
│  └─ README.md                       # 内容格式规范（含 YAML 踩坑）
│
├─ docs/                              # 13 份设计文档（PRD / 架构 / 决策 / 评审 / 报告）
├─ scripts/                           # 3 个构建脚本
│  build-update.mjs  copy-content.mjs  serve-lan.mjs
├─ release/                           # 交付物
│  └─ pks-app-2026-09-10.apk          # （7.35 MB）
├─ .workbuddy/                        # 项目记忆与技能数据（禁止删除）
├─ node_modules/                      # 根依赖（~105 MB）
│
├─ package.json  tsconfig.base.json  pnpm-workspace.yaml  pnpm-lock.yaml
├─ .gitignore  .editorconfig  .prettierrc
├─ README.md  PROJECT_STATUS.md  overview.md  内容更新操作说明-2026-09-10.md
└─ REPORT-2026-09-10-*.md             # 10 份轮次工作记录
```

---

## 二、技术栈与整体架构

### 技术栈

| 层 | 选型 |
| --- | --- |
| 语言 | **TypeScript（strict）**，ESM，`NodeNext`/`bundler` 解析 |
| 前端 | **React 18 + Vite 5**，**纯手写 CSS（无 Tailwind / 无 UI 库）**，自研 hash 路由 |
| 移动端 | **Capacitor 7**（Android；同一套 Web 代码打包） |
| 核心层 | `@pks/core` 同构库：`zod` 校验、`js-yaml`、`unified/remark/rehype`（GFM+数学+sanitize）、`fflate` |
| 工具链 | `@pks/cli`（tsx 运行 TS 源码）、自研分片索引 + BM25 检索 |
| 测试 | **Vitest**（20 文件 / 135 用例，均针对 core） |
| 包管理 | 目录结构为 pnpm workspace，但**本机实际以 npm + 手工 junction 运行**（pnpm 在此环境不可用） |
| Android 构建 | Gradle 8.11 / AGP 8.7.2，**JDK 19**（`D:\JDK\jdk-19.0.1`），compileSdk 35 / targetSdk 34 / minSdk 23 |

### 架构与数据流水线

```
内容源 content/*.md,*.yaml
      │  @pks/cli  build:index
      ▼
content/.index/   （分片索引：manifest / entries 字母分片 / search L1 标题 + L2 BM25 / toc / covers / tracks）
      │  scripts/copy-content.mjs   （投递边界把 .index 改名为 index）
      ▼
apps/web/public/content/
      │  vite build
      ▼
apps/web/dist/  ──►  cap sync android  ──►  android/app/src/main/assets/public/
                                                  │  gradle assembleDebug
                                                  ▼
                                          release/pks-app-*.apk

运行时读取顺序（Web / App 一致）：
  loader 优先读 IndexedDB 内容缓存（若已通过 OTA 激活）
          ⤷ 未命中则 fetch public/content（随包资源）
```

**三条能力主线**

1. **同构核心（`@pks/core`）**：解析（front-matter/正文/标题）、VFS 抽象（`MemoryVfs`/`OverlayVfs`/`zipVfs`/`NodeFsVfs`）、分片索引与 BM25、Track（学习序列）、打包（bundle）、词典——**零 DOM 依赖**，Node 与浏览器共用。
2. **内容工具链（`@pks/cli`）**：`build:index`（索引）、`lint`（8 条规范 L001–L008）、`bundle`（导出分级数据包）、`rename`（slug 级联改名）、`browse:gen`、`search`。
3. **阅读端（`apps/web`）**：沉浸式双层阅读（封面 → 全屏阅读）、划词词典、9 张页面、偏好持久化、**内容更新（局域网 OTA：清单 + 逐文件 sha256 校验 + IndexedDB 缓存层）**、原生返回键桥接、双击退出。

---

## 三、当前完成度评估

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0 / T01–T03 | `@pks/core` + `@pks/cli`（解析 / VFS / 倒排+BM25 / 索引 / Track / 打包） | ✅ 完成 |
| T04 | Web 阅读端（Vite5 + React18 + TS strict，纯 CSS，自研路由，9 页面） | ✅ 完成 |
| T05 | Capacitor 7 Android 打通，debug APK 构建成功 | ✅ 完成 |
| V1–V4 | App 化：4 Tab、沉浸阅读、续读、历史、偏好；类目页/返回手势/设置拆分；真机修复 | ✅ 完成 |
| 修复轮 | 状态栏沉浸、弹层主题、返回逐级、双击退出 | ✅ 完成 |
| 内容 | 101 词条 / 500 章 / ~44.3 万字；5 条学习轨迹；离线词典 | ✅ 达样例规模 |
| 质量加固 | 135 单测；typecheck 三端干净；lint 0/0；本轮死代码与边界清理 | ✅ 完成 |

**综合判断：功能骨架与阅读体验已完备，处于「实现完成 · 待真机复验 + 发布就绪」阶段。**
按“可发布 v1.0”口径，**工程化完成度 ≈ 85%**；剩余大头是**内容规模化**（PRD 目标 ~2000 万字，现为样例量级），而非代码。

---

## 四、尚未完成 / 有待推进的工作

1. **真机复验闭环**（非代码，前置闸门）：状态栏沉浸/常驻、弹层主题、返回链路、双击退出。
2. **正式签名 release 包**：配 `signingConfig` + `assembleRelease`、体积核对（≤80 MB）、混淆/ProGuard、版本号策略。
3. **OTA 决策**：
   - 内容 OTA：✅ 已实现（局域网 `serve-lan.mjs` + IndexedDB 缓存层）；
   - **APK 自更新：仍为占位**（`lib/update.ts` 的 `UPDATE_MANIFEST_CONFIGURED = false`），需公开托管域名，且 Android 静默安装受限，需自建“下载 + 申请安装权限”。
4. **内容规模化**：目标 ~2000 万字，当前 ~44.3 万字，纯内容生产工程量（最大项）。
5. **大内容性能骨架**：分区懒加载 + IndexedDB 缓存（部分已落地）；当前样例无压力，量级上来后需补全。
6. **iOS 平台**：`cap add ios` + 构建/签名（同套代码，未启动）。
7. **数据管理/导入导出完善**：`bundle` 分级导出已有，导入端（本机文件导入）已具备，UI 与流程可再完善。
8. **增强功能**：笔记 / 评论 / 书签真实化（现为占位）。

---

## 五、主要优点与不足

### 优点

- **架构分层清晰**：`core`（同构纯逻辑）/ `cli`（构建工具）/ `web`（视图）职责分明，核心逻辑被 135 单测覆盖。
- **离线优先、零后端起服务**：内容随包 + IndexedDB 缓存 + 局域网 OTA，整套可完全离线运行。
- **依赖克制**：前端**零 UI 框架/零 Tailwind**，路由、状态、组件全自研；包体轻、可控性强。
- **安全意识到位**：Markdown 渲染经 `rehype-sanitize`，UI 层从不拼接未净化 HTML。
- **工程纪律好**：内容 lint（L001–L008）、`typecheck` 三端校验、可复现构建流水线、详尽的 `docs/` 与轮次报告。
- **一致的设计语言**：沉浸阅读、封面→正文双层、划词词典、返回链路，用户体验成体系。

### 不足

- **构建工具链存在脆弱点**：`@pks/core` 以 `"workspace:*"` 声明，npm 无法解析，依赖**手工 junction**；一旦误跑 `npm install`，workspace 链接被重建即断链。
- **配置自相矛盾**：`package.json` 声明 `"packageManager": "pnpm@9.15.9"` 且保留 `pnpm-lock.yaml`/`pnpm-workspace.yaml`，但项目实际以 npm 运行，还存在 `apps/web/package-lock.json`——**三份锁/工作区配置并存**，易误导。
- **测试覆盖不均**：135 用例**全部集中在 `@pks/core`**；`apps/web`（39 文件，含章节导航、缓存、OTA 等关键逻辑）**零自动化测试**。
- **构建产物与源码同处一树**：因项目**不是 git 仓库**，无版本控制兜底，误删构建产物无法回滚；`packages/*/dist` 既是产物又被运行时依赖，边界模糊。
- **根目录较杂**：10 份 `REPORT-*.md` + `overview.md` + 说明文档平铺在根，缺少 `reports/`、`docs/` 归档。
- **`apps/web/src/styles.css` 体量大**（数十 KB 单文件），随页面增长可维护性下降。
- **死代码残留的教训**：本轮仍清理出 5 处死代码与 1 处“写了但未接线”的 prop（`chapterStart`），说明此前缺乏常规的未用符号检查。

---

## 六、可改进方向与建议

### A. 工程健康（优先级最高）

1. **统一包管理口径**：既然本机只用 npm，建议
   - 删除 `pnpm-lock.yaml`、`pnpm-workspace.yaml`，改为 root `package.json` 的 `"workspaces"` 字段；
   - 移除 `"packageManager": "pnpm@..."`；
   - 为 `@pks/core` 改用 npm 可解析引用（或保留 junction 但**写进 README 与 `prepare` 脚本**，防误 `npm install`）。
   > 注意：此项会改动依赖解析方式，**须在确认 npm workspace 正常工作后再合并**，不建议在发布前夜做。
2. **纳入版本控制**：项目当前**无 git**。强烈建议 `git init` + 首次提交（`.gitignore` 已就绪），使清理/重构可回滚。
3. **把“未用符号检查”固化**：新增 `npm run lint:dead`（`tsc --noUnusedLocals --noUnusedParameters`），或逐步在 tsconfig 打开该开关。

### B. 测试与质量

4. **给 `apps/web` 补测试基建**：为纯函数抽离并测试（`linkifyMarkdown`、`extractHeadings`、`flattenChapterList`、`compareVersions`、`relTime`、`contentUpdater` 的清单校验/并发下载、`preferences` 的钳位）。若 vitest 不便引入 web，可考虑把这些**纯逻辑上移到 `@pks/core`**（它们本就无 DOM 依赖），顺带扩大 core 覆盖率。
5. **为构建脚本加冒烟测试**：`copy-content` 的 `.index → index` 改名、`build-update` 的清单生成，均可用脚本断言。

### C. 结构与整洁

6. **归档报告**：把 `REPORT-*.md`、`overview.md` 收进 `reports/`（或在文件名前加 `reports/` 前缀），根目录只留 `README`/`PROJECT_STATUS`。
7. **固化清理脚本**：新增 `npm run clean`（删除 `dist`/`.index`/`public/content`/android build 缓存），避免每次手工 `rm`。
8. **拆分 `styles.css`**：按 `base / layout / reader / components` 分文件（可用 Vite 原生 `@import`），提升可维护性。

### D. 产品与发布

9. **发布就绪清单（release checklist）**：签名、版本号、体积门禁（≤80 MB）、`assembleRelease` 验证、更新日志。
10. **OTA 路线拍板**：明确 APK 自更新“做/砍”，并据此清理 `lib/update.ts` 的占位代码或补全托管与安装权限流程。
11. **内容规模化流水线**：在 `docs/` 补齐“内容生产 SOP”（词条模板、lint 前置、批量导入），把 2000 万字目标拆成可执行的批次。

---

## 附：本轮清理的边界与风险说明

- 所有删除项均为**派生物或纯垃圾**，可经 §〇 命令重建；**未触碰任何源码、内容、配置与交付物**。
- 唯一“构建产物却有意保留”的是 `packages/{core,cli}/dist`（体积小、运行时所依赖）与 `release/*.apk`（交付物）。
- `apps/web/android/` 内**手改的 Gradle 配置**（Java 17 覆盖、`resolutionStrategy.force` 锁 androidx.core 1.13.1 等）**不可由 `cap sync` 重建**，已完整保留——清理时务必只删 `build/`、`app/build/`、`.gradle/`（缓存目录，注意与 `gradle/` 包装器区分）。
