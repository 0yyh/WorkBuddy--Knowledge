# 质量巡检轮报告 · 2026-09-10

> 范围：**代码质量 / 错误处理 / 边界情况 / 文档** 的系统性清理。
> 约束：**不新增依赖、不改架构、不改破坏性行为**；与现有风格/约定保持一致。
> 结论：3 个真实功能 Bug + 3 处错误处理/边界加固 + 5 处死代码清理 + 1 处过时文档修正。

## 验证基线（改动前后每次均复跑）

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck`（core + cli + web） | ✅ 干净 |
| `npm run test`（vitest，@pks/core） | ✅ **135 / 135 通过**（20 个测试文件） |
| `npm run lint`（内容规范） | ✅ **0 error / 0 warn** |
| `vite build`（web） | ✅ 成功（383 模块，517 KB / gzip 159 KB） |
| `tsc --noUnusedLocals --noUnusedParameters`（三包，**仅诊断**） | ✅ **0 条** |

---

## 一、真实功能 Bug（3）

### 1. 详情页点章节「不跳章」——`chapterStart` prop 从未被读取
- **位置**：`apps/web/src/pages/EntryReaderPage.tsx`
- **现象**：`EntryCoverPage` 的「章节目录」点击会跳 `/entry-reader/{slug}?ch=N`，
  路由也正确把 `?ch` 解析进 `route.chapter` 并作为 `chapterStart` 传入；但阅读页
  **只按「续读位置」打开，完全忽略 `chapterStart`** → 点任意一章都进不去该章。
- **原因**：`index` 初始值仅由 `getLastRead()` 决定。
- **修复**：初始 `index` 优先采用 `chapterStart`（有限数则取整并夹到 ≥0），否则回退续读。
- **验证**：`tsc` 通过；URL `?ch=N` → 直达第 N 章。

### 2. 封面章节下标与阅读页 `docs` 数组「潜在错位」
- **位置**：`apps/web/src/pages/EntryCoverPage.tsx`
- **现象**：封面传 `?ch=i+1`（`i` 为 `flattenChapterList(toc)` 的下标），
  但阅读页 `docs` 会**剔除 `container`（卷）**。当前内容无「卷」，故未显现；
  一旦引入卷，`i+1` 就会偏移。
- **修复**：改为 `chapterRows`（running `docIndex`，按**内容章节**计数；
  `container` 渲染为不可点行）。当前内容下与 `i+1` 完全等价 → **零行为变化**，仅消除未来错位。

### 3. 搜索页「最近搜索」不刷新
- **位置**：`apps/web/src/pages/SearchPage.tsx`
- **现象**：`pushRecent(q)` 写入 localStorage，却**从不调用 `setRecent`**（该 setter 是死代码）
  → 清空查询回到空态时，「最近搜索」仍显示旧列表（缺刚搜过的词）。
- **修复**：`pushRecent` 返回更新后的列表，返回处 `setRecent(pushRecent(q))` 同步内存态。

---

## 二、错误处理 / 边界情况（3）

### 4. CLI 索引部分损坏时抛原生 ENOENT 栈
- **位置**：`packages/cli/src/load-index.ts`、`packages/cli/src/commands/search.ts`
- **现象**：`manifest.json` 在、但 `search/title.json` 缺 → `readFileSync` 直接抛栈回溯。
- **修复**：关键文件缺失 → 返回 `null`（调用方提示「先运行 build:index」）；
  文件在但 JSON 损坏 → **抛明确错误**「索引文件损坏（…），请重新运行 build:index」；
  单个 L2 分片损坏 → 按「分片不存在」处理（少召回，不炸整个检索）。`search` 相应 try/catch。
- **验证**：三种状态（缺 / 损坏 / 正常）各只输出一条清晰信息，退出码 1/1/0。

### 5. 内容更新清单为 `files: []` 会「清空整份缓存」
- **位置**：`apps/web/src/lib/contentUpdater.ts`
- **现象**：`applyContentUpdate` 成功后执行「清理不在清单里的旧缓存」；
  若清单 `files` 为空，`keep` 为空集 → **把所有缓存全删**。
- **修复**：`isManifestShaped` 拒绝空 `files`（并同步更新报错文案）；
  `applyContentUpdate` 对 `total === 0` 提前返回且**不激活**。

### 6. `search --level` 非法值静默降级
- **位置**：`packages/cli/src/index.ts`
- **现象**：原 `flags.level === 'l2' ? 'l2' : 'l1'` 会把 `--level bogus` 静默当 l1。
- **修复**：新增 `searchLevel()` 校验，非法值直接报错退出（与 `bundle --level` 一致）。

---

## 三、死代码清理（用 `tsc --noUnusedLocals` 诊断逐一确认）

| 文件 | 清理内容 |
| --- | --- |
| `packages/core/src/index/builder.ts` | `tocHeavy` 循环内未使用的 `const secs = sections[slug] ?? []` |
| `packages/core/src/content/repository.ts` | 未使用的 `normalizePath` 导入、未使用的 `EntryMeta` 类型 |
| `packages/cli/src/commands/build-index.ts` | 未使用的 `join` 导入 |
| `apps/web/src/lib/content.ts` | **全项目零引用**的 `buildChapterTree`（含其内部 `byKey` / `void byKey` 死代码） |
| `apps/web/src/types.ts` | 仅被上者使用的 `ChapterView` 接口 |

> 说明：`tsconfig` 的 `noUnusedLocals` 是**刻意关闭**的，故仅作**临时诊断**，未改配置。

---

## 四、文档修正

- `PROJECT_STATUS.md` §四原记「`setBackInterceptor` 为死代码：按返回不会先关浮层」——
  经复核为**过时结论**：`EntryReaderPage` 注册拦截器，`handleNativeBack` 第 1 步消费它，
  `App.tsx` 装 `installNativeBackHandler`，`MainActivity` 调 `window.__pksHandleBack`，链路完整。
  已改正并加注复核日期。

---

## 五、方法论（可复用）

1. **死代码定位**：`tsc -p <pkg>/tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters`
   可一次性列出三包未使用符号（只诊断，勿改 tsconfig）。
2. **工具坑**：同一条消息里对**同一文件**发多个 `Edit`，出现过「部分生效、部分静默未落盘」；
   改完**必须回读 / `grep` 校验**，缺失就逐文件单独重发。（本轮 3 处因此回补）
3. **不触碰**：`lib/update.ts` 的 APK 自更新占位 URL —— 按既定计划属残留，需公开托管域名后启用。

---

## 六、未处理项（有意保留）

- `lib/update.ts` 的 APK 自更新占位 URL（`UPDATE_MANIFEST_CONFIGURED = false`）——既定残留。
- `covers/{slug}.json` 仍靠 `synthesizeCover` 回退（无真实封面数据）——内容侧事项。
- 评论 / 笔记为占位，未实做——产品范围决定。
- `apps/web` 无自身测试运行器（vitest 仅装在 `@pks/core`，web 端不可解析）；
  为遵守「不新增依赖 / 不 install」，本轮**未**给 web 加测试基建。
