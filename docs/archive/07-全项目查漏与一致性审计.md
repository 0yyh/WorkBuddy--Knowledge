# 07 · 全项目查漏与一致性审计（2026-09-06 收口）

> 用途：用户在前期方案基本齐备后，要求"再详细过一遍整个项目，看有没有遗漏或需补充"。本文档汇总全局复查结论：已修正的内部不一致 + 仍待用户拍板的真实缺口 + 建议但非必需的可选项。仅规划，不写代码。

## 0. 复查结论（TL;DR）

前期方案**结构上已完备、无阻断性缺口**：文档体系 7 份（01/01b/02/03/04/05/06 + 本 07），决策、裁决、模型、技术选型、数据可信、UI/安全、类目规划、任务拆解、里程碑全部覆盖，且交叉一致。本轮只修掉了 **5 处陈旧/矛盾引用**，并筛出 **4 类真正待你拍板的开放点**（多数不阻塞冻结 M0）。

---

## 1. 本轮已修正的不一致（5 处）

| # | 位置 | 问题 | 修正 |
|---|------|------|------|
| 1 | 02 §15 gantt + §15.2 | M5 内容批次仍写"马克思主义 ~120 → 哲学 ~150"，被 01b §7 五类目并行取代 | 改为"B1 五线并开 ~100 → B2 大部头延伸" |
| 2 | 02 §16 假设 A + §17 项1 | 平台仍标"待用户定 iPhone/Android"，实际已定 Android 先行 | 标注 ✅ 已定（决策 #2） |
| 3 | 01 §9.1 | 目录树/注仍用旧 `_by-category/` 软链接视图，被 02 §9.7 `_browse/` 影子树取代 | 改为 `_browse/` + browse.ts；说明否决软链接原因 |
| 4 | 01 §5.2 C-06 / §7.1 | 仍写生成单文件 `search-index.bin` | 改为分片索引 `index/`（对齐 02 §3.4） |
| 5 | 01 §8.1 | 包结构索引分片命名与 02 不一致、漏 tracks/overlay/feedback | 整体重写：bundle.json + content + overlay + tracks(顶层) + index 分片 + feedback + userdata |

> 另：03 §6 五项收尾（A–E）已全部由架构师+产品经理补全并落盘，§6 标"已解决"、§7 Q15 已定案。见各文档。

---

## 2. 真正待你拍板的开放点（4 类，多数不阻塞冻结 M0）

### 2.1 需在 M0 前（或尽早）定的「内容范围」类

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| O-1 | 除五大类目外，B2 起是否先引入 **文学 / 心理学 / 经济学**（06 §5 的 Priority A）？ | 决定 taxonomy 是否需要为一期就预留这 3 个 L1 根节点（预留成本极低） | 建议**一期只做 5 类**，B2 再评估，不拖慢首个可用版 |
| O-2 | 除《资本论》《共产党宣言》外，**还需整本录入哪些大部头**（马恩/毛选/邓选/习选等，见 06 §2.5 + 02 §3.5）？ | 决定公有领域来源采购清单与 M3 录入管线的首批目标 | 建议先只保《共产党宣言》(B0) + 《资本论》第一卷(M2 验收)，其余 B2+ 陆续加 |
| O-3 | 历史双时间线**是否需要"同期对照"显式视图**（世界史↔中国史并排时间轴）？ | UI 复杂度差一个档次（03 §2 提到桌面双轨交叉连线是既有设计） | 已有设计支持；建议一期桌面保留对照、手机单轨切换即可 |

### 2.2 已预留、由你在 M0 后随时可改的「开关」（默认即可，无需现在定）

| # | 项 | 默认值 |
|---|----|--------|
| O-4 | 连续阅读模式默认开关 | 默认**关闭**（按章读），M2 实测后定 |
| O-5 | 词典分词 | 默认 bigram，M1 实测后定是否启用词典 |
| O-6 | 中文内嵌字体 | 默认不内嵌（系统字体栈），P2 再评估 |
| O-7 | 局域网 Wi-Fi 传输（X-05）是否一期做 | 已列入 T03（P1），按需开 |

### 2.3 跨序列语义（03 §7 遗留，二期待定即可）

| # | 问题 |
|---|------|
| Q13 | 一个词条可同时进几条序列？（0..n 皆支持，仅默认值问题） |
| Q14 | 手机端序列页是否提示"同期还有其他序列相关词条"？ |
| Q16 | 多序列时"上一课/下一课"按当前序列还是可切换？ |

### 2.4 内容细节（不影响架构，录入阶段随内容累积微调）

| # | 问题 | 处理 |
|---|------|------|
| O-8 | slug 中译英规则：拼音兜底 + 常用词人工映射表 | 随内容积累维护映射表（02 §17 项9） |
| O-9 | 分词/词典的来源与许可证 | 架构 M1 时选定（如 jieba 词典 BSD-3、CC-CEDICT） |

---

## 3. 建议但非必需的可选项（按需，不阻塞）

- **3.1 内容规模上限**：当前按 500 万字/5000 词条设计；若预期远超此量，需提前考虑 676 分片或引入轻量 DB——**这是唯一可能反向影响架构的参数**，建议先预估一个量级（见 Q 下）。
- **3.2 暗/护眼主题对比度**：05 已含三主题；无障碍（WCAG AA）对比度校验可作为 P1 收尾项。
- **3.3 PWA 降级**：已作 M-10 P2；若担心"不想装 App"，可作为 Web 端优先交付的兜底。

---

## 4. 建议的下一步

1. **你可先回答 §2.1 的 O-1/O-2/O-3**（尤其 O-2 大部头清单），这三项会轻微影响 taxonomy 预留与 M3 管线首批目标，建议 M0 前定，但都不阻塞 T01/T02 基础设施。
2. 若对现状满意，**可随时冻结 M0** → 进入 T01（M0 最小子集已在 02 §15.3 划好，≈14 人日）。
3. 本文档（07）作为查漏审计留档；后续若你补充需求，我据此增量登记，避免方案漂移。

---

*审计人：主理人 齐活林（Qi） · 交付总监*
*日期：2026-09-06*
