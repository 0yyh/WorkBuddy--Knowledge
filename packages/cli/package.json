{
  "name": "@pks/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node 工具链：build:index / lint / bundle / rename / browse:gen",
  "bin": {
    "pks": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pks/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
