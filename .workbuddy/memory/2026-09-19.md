# 2026-09-19 工作日志

## 全站双标记审计收口 + lint L009 规则（提交 d800a7b / 2debd69 / 97f87d7，均已 push Gitee）

### 上午：全站「entry + chapter 双标记」全量审计（d800a7b）
- 对全部 195 词条做 entry+chapter 双标记扫描：entry 195/195 全 V5；chapter 948 中 325 非 V5
  （297 MISSING：worker 批次新建章节没写标记；25 个 V3/V4；20 个 V6；1 个 MULTI：capitalism）。
  分布：哲学 220 / 政治理论 88 / 经济学 15 / 科学 2。
- 同款式幂等归一（strip 三形态 + 追加单 V5）→ 复跑审计 entry 195/195、chapter 948/948 全 V5。
- lint 0/0；commit `d800a7b`（325 files, +555/−103）→ push；memory 另提交 `2debd69`。

### 下午：在 lint 加 L009 标记校验规则（97f87d7）
用户要求"在 lint 里加一条 marker 校验规则"，把"单一 V5 标记"从约定层提升为可校验规则。

- **`packages/cli/src/commands/lint.ts`**：
  - 新增纯函数 `lintMarkerText(text, label): string[]`（可单测，不依赖 vfs）——检测缺标记 /
    残留 V1~V4,V6 / 多重 / 双重注释包裹（DOUBLE）/ 裸 token（未包 <!-- -->），全部映射为 L009 error。
  - 新增 `lintMarkerRule(vfs, slug): Issue[]`——走 vfs 读 `entries/<slug>/entry.md` 与其
    `chapters/*.md` 原始内容，复用 `lintMarkerText`。
  - 接入 `lintCmd`（全量）与 `lintEntryDir`（单条目，T03 生成回路）。
- **`packages/cli/src/gen/parse-output.ts`**：
  - 新增 `normalizeMarker(content)`——剥离所有残留标记形态（double-wrap / 单注释 / 裸 token，
    含 V1~V6）后追加单一 `<!-- PKS_EXPANDED_V5 -->`。
  - 在 `fromJson` 与 `fromMarkers` 两处 `files.set` 前套用，**生成管线产出即合规**。
  - ⚠ 这一步是关键配套：否则 `lintEntryDir` 把 L009 设为 error 会让 gen 自检循环
    对每篇缺标记的生成稿无限重试。parse-output 原本**不**写标记，所以必须让生成端兜底。

### 验证（实测）
- 全量 lint：`🔍 lint 完成：0 条（error 0 / warn 0）`（当前 corpus 全 V5，无回归）。
- `lintMarkerText` 8 例合成用例全过（clean / missing / V4 / V6 / multi / double-wrap / bare / multi-wrong）。
- `parseOutput(JSON)` 实测：entry.md + chapters/ch-01.md 各产出 1 个 V5 标记。
- 临时测试脚本（`_test_marker.ts` / `_test_gen.ts` 等）全部 `unlinkSync` 清理，工作树干净。

### 关键点（下次改 lint / gen 必看）
- lint 命令实际是 `node ... tsx ... packages/cli/src/index.ts lint --content content`，
  cli 经 tsx 直接跑，**不**走 tsc 构建、无 cli 专属 tsc 配置；改 cli 后直接用上述命令跑 lint 验证即可。
- L009  severity = error（目标就是强制）。若日后想对"手工新增但暂未加标记"的内容宽松些，
  可考虑在 `lintEntryDir`（仅 gen 回路）降为 warn，但不要动 `lintCmd`（全量门禁）。
- 本环境 Bash shim 已彻底坏（缺 dirname/cd/ls/find/head/grep/tail/cat），一律用 node -e / Grep / Glob / Read，
  输出重定向到 `.txt` 再 Read，别接任何 `|` 到缺失命令。
