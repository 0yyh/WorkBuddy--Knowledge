# 2026-09-18 工作日志

## P2-12 落地：OTA 内容更新完整性校验（用户选定「防损坏」路线）

用户明确选择**防损坏**（不做非对称签名/防伪造），据此落地端到端校验。两个提交：
`444ba94`（代码实现）+ `9bd8c54`（docs/11 错误技术前提纠正）。

### ★ 核心发现：局域网下旧校验形同虚设（必须记住）
- OTA 走 `http://<局域网IP>`（serve-lan.mjs 监听 0.0.0.0），**不是安全上下文**，
  浏览器不暴露 `crypto.subtle`。
- 旧 `contentUpdater.ts` 检测到 subtle 缺失后**只加一条 warning 就跳过全部 sha256 校验**
  → 那套「逐文件 sha256 校验」在真实 OTA 场景**一次都没生效过**。
- 修复：复用 `packages/core/src/util/sha1.ts`（纯 TS 零依赖、与 node:crypto 有对拍测试）
  作**主校验，永不降级**；sha256 降为 https 场景的**附加**校验。
- 顺带纠正一个认知错误：校验和与内容**同源下发**，只能防损坏、**不能防篡改**；
  docs/11 原写「靠 sha256 防篡改」是错的，已改。防伪造仍需签名（P2-12 遗留）。

### ★ 修复真实 bug：已激活状态下部分失败 → 半新半旧
- `ACTIVATED_KEY` 一旦为 true，新一轮 `applyContentUpdate` 部分失败时只是 return，
  **没有把 ACTIVATED 置回 false** → loader（`loader.ts:54-59` 缓存优先）会读到
  「本轮新文件 + 上轮残留旧文件」的混合内容。代码注释声称避免了，实际没有。
- 修复：动手前先 `setMeta(ACTIVATED, false)` + `invalidateContentCacheFlag()`，
  失败保持 false → 回退随包内容。

### 落地内容
- `packages/core/src/util/sha1.ts`：`toBytes` 改优先用原生 `TextEncoder`
  （原逐字符 push 对 4.7MB 词典索引极慢）。对拍测试 4/4 保证结果不变。
- `packages/core/src/index.ts`：barrel 新增 `export * from './util/sha1.js'`。
- `scripts/build-update.mjs`：每文件新增 `sha1` 字段；manifest 新增
  `files_checksum`（对 `files.map(f=>path\nsha1\nsize).join('\n')` 取 sha1，不含自身）；
  统一以 UTF-8 文本为校验基准并对齐浏览器 BOM 剥离；`size` 改为文本字节数。
  ⚠ 改动摘要格式必须**两侧同步改**，否则互相判为损坏。
- `contentUpdater.ts`：新增 `computeFilesChecksum` / `byteLengthOf` / `verifyEntryText`；
  校验顺序 size → sha1 → sha256；清单缺 sha1 直接报错（要求重新生成包）。

### 验证（实测，非推断）
- core 183/183；web **67 → 81**（新增 14 用例）；web tsc 0 error；lint 0/0。
- **端到端**：真实生成 1401 文件 / 16.42MB 更新包，用消费端同一实现校验
  **零不符**，全量校验耗时 **810ms**；篡改 1 字符、清单少一项均能被检出。
- 内容文件 **0 个 BOM**（1401 个实测），假设成立。

### 新增用例要点（下次改这里必看）
- 测「SHA-1 校验」必须用**等长篡改**（如 `hello 中文`→`hello 中英`），
  否则会先被 size 预检拦下，根本测不到 SHA-1 那一层。
- 「部分失败」用例现断言 `activated === false`（不再是 undefined）——
  因为开头已先置 false。

### 仍遗留
- 防伪造签名（P2-12 剩余部分，可信局域网可接受）
- `useWindowedSlice` / `compress` / `df.ts` 缺直接单测
- `builder.ts` 469 行 + df 桶每次全量重算压缩；CI 不构建 Android

### 环境补充
- vite 真实路径：`node_modules/.pnpm/vite@5.4.21_@types+node@22.20.1/node_modules/vite/bin/vite.js`
  （也可能是 `apps/web/node_modules/vite/bin/vite.js`）。web build 需
  `CODEBUDDY_SAFE_DELETE_ENABLED=0` + 后台跑。
- core 重编 tsc 在 **packages/core 下**要用 `../../node_modules/.pnpm/typescript@5.9.3/...`
  （core 自己 node_modules 下没有 typescript）。
- 本轮两个调研 worker（general-purpose-1-2 / -2-2）**均 429 限流失败**
  （02:01:45 重置），调研与实现改由主理人直接完成。
