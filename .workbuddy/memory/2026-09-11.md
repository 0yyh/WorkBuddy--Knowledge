# 2026-09-11

## 阅读器底栏番茄风 + 残留透字修复（承接 09-10 深夜的设置面板修复）
用户反馈「还是没有完全修复」并附 4 图：红框标注章节条与底部导航之间正文文字透出；并要求底部导航栏样式对齐番茄小说（参考图 4：细线图标、无分隔线、底色随主题）。

根因与修法（commit `5b30328`）：
- **透字根因**：`.reader-bottom`（章节条）与 `.reader-statusbar` 用的是半透明 `--reader-chrome-bg`（0.92~0.94），关「展示进度时间和电量」后状态条区域收缩，正文从半透明底透上来。`.reader-bottom-tabs` 虽是实色但硬编码 `#ffffff`，不随主题。
- **修法**：三者在`.reader-bottom` / `.reader-bottom-tabs` / `.reader-statusbar` 统一改 `background: var(--reading-bg)`（实色、随 7 主题自动适配）；分隔线用 `var(--rp-border)`；深色系硬编码覆写（`#1f1f1f` 底 / `#e6e6e6` 字）整块删除——变量化后不再需要。
- **番茄风图标**（EntryReaderPage.tsx 内联 SVG，strokeWidth 2→1.5，全描边无填充）：
  - 目录：书本框 → 三横线列表；夜间：填充月牙 → 线框月牙（夜间态仍为线框太阳）；设置：复杂齿轮 → 六角螺母+中心圆。
  - `.reader-tab` 颜色 `#333` 硬编码 → `var(--rp-soft)`；去掉 Tab 间竖线分隔（`:not(:first-child) border-left` 删除）；`:active` 按压底色改 `rgba(127,127,127,0.12)`（深浅主题通用）。
- **验证**：typecheck EXIT 0；build 成功（CSS 79.11kB / JS 528.38kB）；e2e-reader-refactor 全绿 exit 0；e2e-search-worker 全绿 exit 0；另写临时 CDP 截图脚本（用后已删）实拍 412x915 视口确认：新图标生效、章节条/底栏/信息条无透字、背景融合。
- **遗留待确认**：番茄参考图底栏有第 4 个 Tab（截图模糊无法辨认是「缓存/更多/听书」），PKS 当前 3 Tab（目录/夜间/设置）无对应功能，未擅自添加——已向用户提出，等指定功能后再加。

## 两个顽固 bug 根修（commit `07472ce`，2026-09-11 中午）
- **① 裂隙（关进度信息后章节条/面板与导航栏之间露正文 30px）**：真根因是 **CSS 自定义属性在「声明元素处」完成 var() 替换后按计算值继承**——`--reader-nav-h` 原声明在 `:root`，statusbar-h=30px 被烤进继承值；`.reader-no-statusbar` 覆写 `--reader-statusbar-h:0` 只影响直接引用它的导航栏/信息条，消费 nav-h 的章节条/`.reader-sheet`/子层 inset/`.reader-scroll` padding 全都不重算 → 上层不下沉、导航栏下沉，裂出一条缝。前两轮（挂类到 .reader-root / body）都只修了「直接引用」路径故不彻底。**修法**：`--reader-nav-h` 改为在 `.reader-root, body` 上计算（`:root` 只留两个高度基量）。此教训通用：**凡子作用域要覆写变量参与 calc 的，calc 必须放同一作用域声明**。
- **② 切章后点中央唤起失灵**：`[index]` effect 先 `scrollTo(0)` 后 `setChromeDismissed(false)`，但 scroll 事件异步晚到 → `hideChrome()` 见 overlay=true 把 chrome 重新滚隐 1.8s → 切章后「overlay 在而卡片不可见」，第 1 点把不可见 overlay 关掉、第 2 点打开时可能仍 dismissed → 需多次点击。修法三件套：`chromeSuppressRef` 抑制旗标（`[index]` effect 置位、hideChrome 消费、新章 load `.then` 复位——防旧内容已在顶部时 scrollTo 不发 scroll 事件导致旗标滞留）；`[index]` effect 补清 chromeRevealTimer；`onSurfaceClick` 唤起 overlay 时强制清 timer + `setChromeDismissed(false)`（保证首次点击必可见）。
- **验证**：临时 CDP 行为探针（用后已删）量化断言全过：关进度信息后章节条↔导航栏 gap=0px、设置面板↔导航栏 gap=0px、body 有 reader-no-statusbar；overlay 唤出态点「下一章」切章后 dismissed=false 且章节条 opacity=1；从关闭态点中央 1 次即唤起（dismissed=false、opacity=1）。typecheck EXIT 0、build 成功、e2e-reader-refactor ok:true、e2e-search-worker ok:true。实拍截图确认无裂隙。

## 首页 V7 类目导航收敛（commit `75b1d70`，2026-09-11 下午）
用户需求：移除首页独立的「全部类目」折叠树（随内容增长愈发臃肿），子类目导航收敛进大类卡片——点卡片进入该主题的子类目列表与内容页。

实现（3 文件，+90/-331）：
- **Home.tsx**：删 `CollapsibleCategoryTree` / `groupDomId` / `scrollToGroup`；L1 卡片 button→`<Link to=/browse/:id>`（`.l1-card` 补 `color:inherit` 防链接色）。
- **BrowsePage.tsx**：标题下新增「子类目」网格（`node.children` + `categoryCounts` 计数，`.browse-sub-card` 双列/≥701px 三列），下方仍为该类目含子孙的全部词条。
- **styles.css**：清光折叠树死样式（`.home-tree` 两处、V5 §5-§8 的 l2-*/l3-* 族、V6 的 l1-head/l2-head 族），新增 browse-subs 样式。
- **验证**：临时 CDP 探针 8 断言全过（首页无 l2/l3 残留、卡片全为指向 browse 的 `<a>`、点卡落地 `#/browse/history`、子类目网格计数正常、二次下钻 hash 逐层变化）；412x915 实拍首页+类目页；typecheck EXIT 0；双 e2e 回归 ok。证据图：`.workbuddy/home-v7-cards.png`、`browse-subs-page.png`。
- **踩坑重演**：styles.css 多个 Edit 并行发同文件 → 又出现「报成功但未落盘」（§6-8 与媒体查询两处漏改，grep 复查抓到后顺序补齐）。**同文件多处改动必须逐条顺序 Edit 并回读/grep 复核**。

## 内容充实：新增 14 词条（纯内容，未动样式）
用户指示「整体样式暂不改，继续补充词条内容充实项目」。筛出 11 个空缺词目（避开已存在的康德/笛卡尔/量子力学/工业革命等）：哲学 8（thomas-aquinas/john-locke/spinoza/socrates/empiricism/rationalism/mencius/zhu-xi）、科学 3（relativity/big-bang/genetics）、经济学 1（keynes）、政治理论 2（communism/machiavelli），每词条 3 章（tldr+keyPoints+正文）。

生成器踩坑（用后即删的 `scripts/tools/gen-entries.mjs`，已删）：
- 章节 front-matter `summary.keyPoints` 缩进错成 4/6 空格（应为 2/4），YAML 解析失败；**lint 的 YAML 解析器部分宽松**——首次只报 thomas-aquinas(3)+zhu-xi(3) 共 6 条，实测 socrates/keynes 等同样坏（肉眼可见），全部 42 章须修。
- `created_at/updated_at` 误用 `e.date`(undefined) → 修正为 `DATE` 常量。
- `communism` see_also 指向不存在的 `marxism` → 改 `chinese-marxism`（L006 报错）。
修法：改生成器 3 处 → 删 14 目录重跑 → 重生。结果：`npm run lint` **0 错 0 警**；`build:index` 115 词条/542 章/450889 字；`content:copy`（须 `CODEBUDDY_SAFE_DELETE_ENABLED=0`，否则 safe-delete 拦 `public/content` 报错）115 词条入 web 副本。`.index` 与 `public/content` 均 gitignore，仅提交 14 词条源目录。
