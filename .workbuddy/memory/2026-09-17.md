# 2026-09-17 工作日志

## PKS 项目综合评估（六维度）
主理人（software-team-lead）对 PKS 知识站做全面评估，四个并行只读子任务：
- 架构/技术栈/可扩展性 → software-architect（已交付）
- core/cli 代码质量与性能 → software-engineer-2（已交付，df 分片化落地复核）
- 构建/APK/内容管线 → general-purpose-1（已交付）
- Web 前端质量/性能/可维护性 → 沙箱 Agent 分派两次 429 失败 + SE-6 错用旧任务上下文，最终由主理人直读 apps/web 补全

### 关键已核实事实
- df 分片化(T03)已落地：packages/core/src/index/df.ts（DF_BUCKET_COUNT=64）、builder 改写 df/meta.json+bucket-NNN.json 替代单一 48MB df.json；搜索 worker 按需懒加载。提交 396f681（本地未推送），155/155 core 测试过。
- 分类计数「标签语义/并集去重」改造已提交 a00ea6d（SE-6 验证：tsc 0 error、loader.test 12/12 通过、vite build 成功；哲学/美学=11、形而上学=10 等计数已修正）。
- APK v1.7 已构建验证（182 词条/879 章节/143 万词）。
- Web 包体积：main 401KB/gzip129KB、EntryReaderPage 95KB/gzip25KB、search.worker 9.4KB 独立 chunk、CSS 79KB（纯 CSS 零框架）。

### 主要风险（评估结论）
- P0：无 CI/lint 门禁（无 .github/workflows、无 eslint）；增量构建 tryLoadIncremental 零测试；搜索缓存无界（真机 OOM 风险）。
- P1：增量构建仅覆盖 postings，df 每次全量重算；copy-content 每次全量重拷 content（20M 字下主导耗时）；entry 26 字母分片失衡；Web 列表（Browse/Search）无虚拟化；df 桶未压缩；版本号硬编码双处易漂移。
- P2：builder.ts 264 行上帝函数、magic number 散落、frontmatter 校验 mutate 入参、检索合并/df 聚合缺单测、纯 CSS 79KB 规模维护。

### 沙箱踩坑（本次复现）
- 本沙箱 Agent 子任务分派遇 429 限流（2026-09-17 ~19:54 UTC+8 重置）；SE-6 被 SendMessage 唤醒后复用旧 paused 任务上下文而非新派评估。教训：限流恢复后再派；新任务要在消息里显式覆盖旧上下文。
