# 2026-09-06 工作日志

## 知识学习站项目前期方案（软件团队 SOP：PM→架构师）

**目标**：用户要做纯本地的「知识学习站」——电脑 Web + 手机 App（Capacitor 一套代码），五大学科（哲学/历史/科学/金融/政治理论）阅读浏览，内容 AI 抓取录入。当前阶段=前期方案，不写代码。

**关键决策（已与用户确认）**：
- 技术：React+Vite+TS 一套，Capacitor 打包；Android 先行，iOS 二期
- 内容=普通文件夹+Obsidian 编辑（不做网页写盘）；马列著作=原文+AI导读+难句注释（`ai_annotated`）
- 一期=五类目并行铺开；纯文本无图片（P1）；顺序=AI草案+手工微调；首交付=B0 先跑通导出→手机→导入→可读链路

**核心架构判断（来自成员）**：
- 两轴模型：分类轴(树) × 顺序轴(Track 线性) 正交；词条=(分类坐标,序列坐标)
- C1：内容目录按 slug 扁平化，类目仅存 front-matter `categories[]`（解决多归属"双份真相"）
- C2：基线随包走（rev + base_snapshot + imports.json 三方比对）
- 存储不引数据库：正文只读文件 VFS Overlay + 索引分片 JSON/bin 入 Worker；搜索=自研 CJK bigram 倒排+BM25 分16片；长文 chunk 化挂载(8k/块)不用虚拟滚动；AI 管线跑 Node CLI，公版原文不调 LLM
- 里程碑 M0 冻结→M1 骨架→M2《资本论》真机(最高风险)；任务约 26 人日

**交付文档**（D:\WorkBuddy--Knowledge\docs\）：
- 01-PRD-知识学习站.md（已含 C1-C7 裁决）
- 01b-PRD补充-学习顺序与类目扩展.md（Track 模型 + 历史双时间线）
- 02-架构设计-知识学习站.md（M0-M5、假设A-D、C1-C7）
- 03-决策与整合总览.md（主理人汇编）

**注意**：子智能体因 API 限流(429)中断于第二轮精修；§6 还有 5 项待收尾（扁平目录可维护性、Track编排CLI、M0最小子集、PRD残留 search-index.bin、tracks/ 进包一致性）。限流约 2026-09-07 00:55 UTC+8 解除，可让成员补全。

## 第二轮讨论：数据可信 + 端共通 + 内置 AI（21:45）

用户接受我提的全部建议，要求补进方案。产出 `04-数据可信与修正机制设计.md`：
- 数据可信：front-matter 加 `sources[]`/`ai_generated{}`/`status`/`verified_immune`；三层叠加（基础层 AI 草稿 / 修正层 用户校订 overlay / 渲染合并）；校验状态机 auto→flagged/verified/corrected；免疫标记防刷新冲掉校订；多视角并存（政治理论争议话题）；3-way 合并；版权时效约束。
- 端数据共通：确立"电脑主编辑、手机主阅读"；共通靠导入/导出数据包，修正层随包走，手机生成的修正可回导电脑；一期手工搬包，二期可选 WebDAV/网盘。
- 内置 AI 答疑窗口：选中存疑段落直接问，采纳写入修正层。**关键张力**：与"纯本地离线优先"冲突——推荐一期用云端 API（用户自备 key），仅答疑联网；二期评估本地小模型。新增待确认 Q17-Q21。
- 更新 03 总览 §9 引用 04，并标记 Track 进包(E) 已解决。
- 注意：用户已取消 SoftwareCompany 专家模式（后又在 craft 块里重选，矛盾）；本轮直接以助手身份深度讨论，未再调度子智能体（仍限流）。

## 第三轮：技术路径/UI/性能/安全（22:08）

用户取消"内置 AI 答疑窗口"（仅取消 App 内答疑；桌面端 AI 抓取录入 CLI 保留，不进 App）。产出 `05-技术路径与UI性能安全.md`：
- 技术骨架 verdict：能胜任，但补 3 纪律——①渲染强制 sanitize（防 XSS，安全硬门槛）②性能预算量化（FCP≤500ms/TTI≤1s/60fps）③UI 库减重（MUI→Tailwind+headless）。
- UI 技术：React18+Vite+TS；react-markdown+rehype-sanitize；Zustand；状态不进 React；动画仅 transform/opacity。
- 布局：桌面三栏（类目树/正文/TOC）；手机底部3Tab+面包屑+沉浸阅读+搜索FAB。
- 秒开：本地零网络 + 原生 splash + Shell 先交互 + 索引 Worker 后台。
- 安全核心结论：系统沙箱已隔离其他 App；真正风险在"内容渲染(XSS)+权限"；对策=离线无网络权限+sanitize+文件私有目录。强烈建议一期 App 不申请 INTERNET 权限。
- 更新 04：移除内置 AI（§3 取消、Q17-Q21 作废、内容 CLI 保留）。
- 新增待确认 Q22-Q25（UI库/纯离线无网权限/护眼主题/底栏隐藏）。

## 第四轮：A/B/C/D 确认 + 类目规划（22:19）

用户取消内置 AI 答疑后（仅取消 App 内答疑；桌面 AI 抓取录入 CLI 保留），对 A/B/C/D 与类目规划给最终指示，全部"按助手建议"接受：
- A 细化（密度轴/两级提炼）：大部头先要点再原文、章节前再提炼；数据模型加第四维度密度轴（著作级 summary + 章节级 section.summary + 全文）。写入 04 §2.5，计入 06 §3 工作量。
- B 标记问题回传：App 内标记随包回电脑；04 §2.6 定义 feedback.json + 闭环。
- C 内容验证：T1 权威典据批量 verified，T2/T3 默认 auto 抽看；落地于 04 §1.6 + §2.4.5 版权三档。
- D Q22-Q25 固化：轻量栈弃 MUI / 一期纯离线无 INTERNET 权限 / 含护眼主题 / 阅读时底栏隐藏；05 §6 由"待确认"改"已确认"。
- 新建 06-类目内容规划.md：五大类目 L2 骨架+顺序轴编排+密度轴落点；B0/B1 排产（含两级摘要×2）；taxonomy.yaml 驱动扩展机制；通识大家建议新增类目（A:文学/心理/法律/艺术/经济；B:医学/地理/计算机/宗教/语言；C:逻辑/军事/环境）。
- 更新 03 总览 §9（Q17-Q21 作废修正）+ 新增 §10 第四轮汇总。

**当前前期方案文档体系（6 份）已齐**：01/01b/02/03/04/05/06。下一步=补完 03 §6 五项收尾 → 冻结 M0 → T01 实现。

## 阅读辅助功能簇（22:45）

用户提议内置词典：阅读时画线选中生僻字/成语/英文→查释义，采纳助手建议再加 划线高亮+批注、术语悬浮卡、复制/分享（离线 TTS 不选）。已落盘：
- 01 §5.1.1 新增"阅读辅助簇"；R-20 内置离线词典(P0) 新增；R-12/R-14/R-17 由 P1/P2 升 P0；R-16 TTS 留 P2。
- 05 §2.4 选区工具条设计：四功能共用一个选区手势（查词/高亮/复制/标记问题），轻量浮层不进 React 重渲染。
- 03 §10 补记；词典数据随包内置、Worker 后台加载、零网络（强化无 INTERNET 权限决策）。仍属"阅读辅助"非打卡，符合只要阅读浏览定位。

## 03 §6 五项收尾落盘 + 全项目查漏（23:00-23:20）

并行调度架构师+产品经理补全 03 §6 五项，均落盘：
- A 扁平目录（slug 规范/rename 级联/_browse 影子树）→ 02 §9.6/§9.7
- B Track 编排 CLI（pks track:gen/ai/finalize，4 枚举）+ 网页编辑回写边界（拖拽只写 tracksOverlay 本地 overlay，永不回写正式 YAML）→ 02 §6.7/§6.8
- C M0 最小子集 ≈14 人日（T01-05 mini，对齐 B0，R3 chunk 推迟 M2）→ 02 §15.3
- D 清理 search-index.bin + §8.1 分片包结构（bundle.json+content+overlay+tracks+index 分片+feedback+userdata）→ 01 §5.2/§7.1/§8.1；同步 §9.1 _by-category 软链接→_browse 影子树
- E tracks 随包走已解决（01 §8.1 顶层 tracks/）
- 03 §6 标"五项全部解决"，§7 Q15 定案（网页拖拽→overlay），§8 更新
- 修 02 陈旧引用：gantt M5 批次（马克思主义先行→五线并开）、§15.2、假设 A/§17 项1 平台改"已定 Android 先行"

## 全项目查漏审计（23:20）

应"再详细过一遍全项目看遗漏"，新建 07-全项目查漏与一致性审计.md：
- 已修 5 处陈旧/矛盾：02 gantt M5 批次、02 假设A/§17平台(已定Android)、01 §9.1 软链接→_browse、01 C-06/§7.1 search-index.bin→分片、01 §8.1 包结构重写。
- 待用户拍板 4 类：O-1 六类目是否B2加文学/心理/经济；O-2 除宣言/资本论外大部头清单；O-3 历史同期对照视图；+ Q13/14/16 跨序列、内容规模上限、词典许可证等。
- 结论：结构完备无阻断缺口，多数不阻塞冻结 M0。
- 文档体系现 7 份：01/01b/02/03/04/05/06/07。

## 第五轮：O-1/O-2/O-3 + 超大规模（23:26）

用户拍板 4 项：
- O-1 类目重组：经济学升 L1(含金融子类)；心理学并入科学(人类认知与心理 L2)。根 L1 实际为 5 个(历史/哲学/科学/经济学/政治理论)。
- O-2 大部头：毛选/邓选/江选/胡选/习选 + 党的会议决定/纪要整本录入，以摘要+导读为主、尽量全文（版权三档：公有领域篇目全文、受保护篇目摘要+权威引用片段）。归政治理论"中国政治理论"Track。
- O-3 历史双时间线要"同期对照"显式视图。
- 超大规模：正文将超 500 万、可能到 2000 万字。
调度架构师(software-architect-3)做 2000 万字容量裁决、产品经理(software-product-manager-3)做类目重组。架构师已回(未落盘)。
架构师容量裁决要点：检索分区 16→256(2 的幂)；entries 元数据暂留 26 片；L1 升格唯一常驻秒开索引(去 summary)；不能全量载入 Worker(改分级常驻+按需分区懒加载+LRU+IndexedDB 磁盘缓存，Worker≤128MB，不引 SQLite)；正文随 apk 不可行→三层分发(Seed 随包/学科摘要包/学科全文包)；apk≤30→≤45MB；秒开前提=首屏与全文索引彻底解耦；分层内容升 P0。
用户对架构师 3 个量级问题拍板：章节多为摘要层(非数十万章全文)；接受全库深检索低频可等待；Seed 只保六类活跃摘要。已写入 03 决策 #11/#12/#13。

## 第六轮：两份裁决落盘 + 全维度查漏（2026-09-07）

- 落盘：产品经理类目重组裁决全文 → 06 §7（含 taxonomy 骨架/categories 迁移/对照视图/政治文献两形态/B0-B1 排产）；架构师 2000 万字容量裁决全文 → 02 §18；05 §4 冷启动链修订；01b 顶部 v2 supersede 横幅。
- 新建 08-全维度查漏补全报告.md：列 12 项缺口（B1-B12）+ 4 项待拍板(C1-C4)。★需 M0 前定：B1 汇编体 Section 树/ B2 学科索引包粒度/ B4 cross_timeline 字段/ B7 section.summary 截断口径/ B11 大文件传输；C1 apk≤45 vs ≤30、C2 金融快捷入口、C3 选集建模口径。
- 文档体系现 8 份：01/01b/02/03/04/05/06/07/08。



