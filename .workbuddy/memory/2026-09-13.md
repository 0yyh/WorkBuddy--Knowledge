# 2026-09-13

## 小说阅读页两处修复：字数口径统一 + 面板动画性能

### 1. 字数统计口径不一致（详情页总字数 ≠ 章节字数之和）
- **根因**：`packages/core/src/index/builder.ts` 的 `w` / `cover.word_count` / `titleIndex.words` / `stats.words` 只统计 `entry.md` 导读正文，章节字数在别处累计 → `hegel` 详情页显示 1775，而 4 章实为 8411。
- **修复**：builder 统一为一个口径 `entryTotalWords = 有内容章节时 Σ(非 container 章节 words)，否则回退 entry.md 正文`；container（卷）行不计入、也不显示，避免与子孙双算。`w` / `word_count` / `titleIndex.words` / `totalWords` 全部由它派生。
- **UI 兜底**：`EntryCoverPage.tsx` 的「字数」= 目录中 content 行 `ch.words` 逐行求和（与下方展示严格一致）；无内联目录（超长词条）时回退 `cover.word_count`。
- **校验**：全量扫描 122 词条，`w === Σ(非 container 章节 words)`，0 不一致；hegel 8411 = 2442+2523+2699+747。
- **章节增/改/删后刷新**：字数在构建期计算，跑 `npm run build:index`（增量按内容指纹重算该词条）即同步；若怀疑产物陈旧，`npm run pks -- build:index --full` 强制全量。

### 2. 呼出目录/设置/间距/更多面板掉帧
- **根因（4 条）**：① `.reader-sheet` 上 CSS `@keyframes sheet-up` 与 `BaseSheet` 的 inline `transition` 双机制并存（关键帧优先级高于 inline style → 入场期间覆盖 inline，且两套时长/曲线不同）；② `@keyframes sheet-up` 在 styles.css 有**两份同名定义**，「后者胜出」→ 实际生效的是无 opacity、无 translate3d 的那份；③ 遮罩 `backdrop-filter: blur(2px)` 每帧重算背景快照；④ `useSheetDrag` 每次 pointermove 都 `setTranslateY` → 每帧 React 重渲染（目录面板可达上千节点）。
- **修复**：`tokens.css` 新增 `--sheet-duration/-ease-in/-ease-out`、`--mask-duration/-ease`；面板位移改**单一 transform 过渡**（挂载后双 rAF 再展开，把首帧重排与动画启动错开）；`closing` 由 `open` **派生**（消除"回弹一帧再下滑"）；遮罩改 opacity 过渡 + 去模糊；`useSheetDrag` 改 rAF + 直写 `el.style.transform`（拖拽全程 React 只渲染 2 次）；`.reader-sheet`/`.reader-more-sheet` 加 `will-change:transform`、`overscroll-behavior:contain`；`.chapter-group` 加 `content-visibility:auto`。
- **新增** `apps/web/src/components/sheet/SubSheet.tsx`：字体/间距/更多三个子层共用壳。原先三子层是父级条件渲染、直接摘除，**没有收起动画**；现改「常驻渲染 + open 驱动」，由 SubSheet 播完退场再卸载（`EntryReaderPage` 三处调用同步改造）。
- **样式对象键顺序铁律**：`{ transition, transform }`（transition 在前）。React 按对象键序逐个 setProperty，顺序颠倒会用上一个状态的曲线（收起时走成入场曲线）。
- 删除 styles.css 中一份被覆盖的同名 `@keyframes sheet-up`（**末尾那份被 `.confirm-sheet` 使用，必须保留**）。

### 验证
- `npm run typecheck` ✅ / `npm test`（core 159 项）✅ / `npm run build`（web）✅
- `node scripts/tools/e2e-reader-refactor.mjs` → **93 断言全绿**（原 52 条 + 新增 N4a-c、A1~A11b、A12、W1~W5）
- 验证同时刷新了 `content/.index/` 与 `apps/web/public/content/`（字数修复需重编 core dist + 重建索引 + 投递）。

## P2（评估 §四 9–13）推进：9/10/13 完成并推送，11 未做，12 暂缓

### ⚠ 新环境故障：`npm run` 脚本全部无法执行
- 现象：`npm run <任意脚本>` → `/usr/bin/env: 'bash': No such file or directory`，exit **127**（shell shim 进一步退化，此前只是 `rm/tail/ls/cd/dirname/head/grep` 挂，现在 npm 也拉不起脚本 shell）。
- **绕过办法（已验证可用）**：不要走 npm script，直接用 node 调 `.pnpm` 里的真实入口（在仓库根执行）：
  - core 重编：`node node_modules/.pnpm/typescript@5.9.3/node_modules/typescript/bin/tsc -p packages/core/tsconfig.json`
  - 建索引：`node node_modules/.pnpm/tsx@4.23.13/node_modules/tsx/dist/cli.mjs packages/cli/src/index.ts build:index`
  - 投递内容：`node scripts/copy-content.mjs`（根目录脚本是 `content:copy`，web 下是 `copy:content`）
  - web 构建：`CODEBUDDY_SAFE_DELETE_ENABLED=0 node node_modules/.pnpm/vite@5.4.21_@types+node@22.20.1/node_modules/vite/bin/vite.js build apps/web`（**root 走位置参数**）
  - web typecheck：`node node_modules/.pnpm/typescript@5.9.3/node_modules/typescript/bin/tsc -p apps/web/tsconfig.json --noEmit`
- 长输出一律 `> X.log 2>&1` 再 Read；**切勿 `| tail`**。日志用完 `node -e "require('fs').rmSync(f,{force:true})"` 删（会被 git status 看到）。

### P2-9 + P2-10：阅读页拆分 + 魔法数字集中（commit `4f9b3e0`）
- `EntryReaderPage.tsx` **976 → 568 行**，成为薄编排层。新增 4 个模块：
  - `apps/web/src/hooks/useReaderScroll.ts` —— 滚动/翻章/信息栏滚隐/章末自动加载全部 DOM 交互（进度条 ref 直写 DOM、`chromeSuppressRef` 抑制切章 `scrollTo(0)` 的异步 scroll 竞态、`pendingScroll` 跳小节）。
  - `components/ReaderChrome.tsx` —— 顶栏/章节条/3 等分导航栏/底部信息条（e2e 选择器类名一个没改）。
  - `components/ReaderToc.tsx` —— 目录搜索 + 章节/小节列表（`chapterNo` 对 `kind!=='main'` 才自增，卷不占号）。
  - `lib/reader-constants.ts` —— `SWIPE_X 60` / `STATUSBAR_H 30` / `CHROME_REVEAL_DELAY_MS 1800` / `AUTO_NEXT_DEBOUNCE_MS 400` / `CLOCK_INTERVAL_MS 1000` / `SWIPE_Y_RATIO 1.2` / `TAP_LEFT_RATIO 0.26` / `TAP_RIGHT_RATIO 0.74` / `SCROLL_BOTTOM_THRESHOLD_PX 4` / `HEADING_SCROLL_OFFSET 12`，**仅搬迁不改值**。
- **踩坑**：`@types/react` **18.3.31** 下 `RefObject<T>` 已收紧为 `{current:T}`。子组件 prop 写 `RefObject<HTMLInputElement | null>` 再传给 DOM `ref=` 会报 TS2322。解法：**照抄 `scrollRef` 的写法**，prop 用 `MutableRefObject<HTMLInputElement | null>`（scrollRef 同款写法本身不报错）。
- 验证：web typecheck ✅；全链路构建 ✅（400 modules，EntryReaderPage chunk 95.5 kB，worker 9.43 kB）；`e2e-reader-refactor.mjs` **93 断言全绿**。

### P2-13：文档治理（commit `4c88dfe`）
- `docs/02` 早已写明「纯 CSS + 设计令牌、零 UI 框架」（§10/§144-146/§818-819），**该子项无需再动**。
- `docs/14-重构引用路径台账与回归验证基线.md` 漂移最严重：仍称「**非 git 仓库、无回滚能力**」（现已 git 化）、文件清点 38（实为 **49**＝components 14 / lib 18 / hooks 1 / pages 9 / state 2 / 根 5）、`EntryReaderPage.tsx` 行号全失效。已在文首加**过期校正横幅**（表格列明三项过期项 + P2-9 新增模块），并声明 §1 台账 / §2 高危点 Top1-10 / §3.4 巡检方法论仍然有效。
- 已推送：`671ef38..4c88dfe master -> master`（gitee）。

## P2-11：补齐 core/web 测试（commit `6180ef4`，已推送）

用户要求「直接做 P2-11」，不再走委派。core **159→181**（+22）、web **0→36**（20 条新增 + 16 条既有但从未跑起来）。

### 最大发现：web 测试基础设施一直是坏的，写了也跑不起来
`apps/web/vitest.config.ts` 两处死因叠加：
1. `import { defineConfig } from 'vitest/config'` —— vitest **只装在 `packages/core`**（apps/web 与根 `node_modules` 都没有），从 apps/web 解析抛 `ERR_MODULE_NOT_FOUND` ⇒ **配置文件根本加载不了**，vitest 直接 Startup Error 退出。
2. `environment: 'jsdom'` —— 本仓**未安装 jsdom**（`.pnpm` 下无 `jsdom@*`），即便配置能加载也会崩。

修复：配置改导出**普通对象**（`defineConfig` 仅类型辅助，运行期不需要）；environment 改 `'node'`；新增 `apps/web/vitest.setup.ts` 补 localStorage/sessionStorage 内存垫片，并**把 `window` 指向 `globalThis`**。

### 一个隐蔽的坑（值得记）
`preferences.ts` 的 `safeStorage()` 取的是 **`window.localStorage`**（`typeof window === 'undefined' || !window.localStorage → return null`）。node 环境没有 `window` ⇒ 返回 null ⇒ `readSysPrefs` 静默回退默认值。
**症状**：`sysPrefs.test.ts` 写入 `'dark'` 却读回默认 `'gray'`——看起来像「偏好解析有 bug」，实际是测试环境缺 `window`。只补 localStorage 不够，必须同时补 `window`。

### 新增测试
- `packages/core/test/shard-codec.roundtrip.test.ts`（12）：补既有 `shard-codec.test.ts` 未覆盖的三块 —— `inlineIndexToMap` 旧内联倒排转 Map（此前零覆盖）、中文/CJK/emoji term 的 utf8 多字节往返、版本号校验与损坏输入失败路径。
- `packages/core/test/bm25.regression.test.ts`（10）：金值钉死 BM25 —— 长度归一化使**短文档（beta, tf=1）胜过 tf 更高的长文档（alpha, tf=2）**；打分确定性；P0-II 全局 stats 覆盖分片内统计；全局 df 缺失时回退 `postings.size`。**金值由 `bm25Term` 实际算出而非手写**（node 动态 import `dist/index/bm25.js` 取值后写入断言）。
- `apps/web/src/lib/loader.test.ts`（12）：`fetchShardText` 的 URL 拼装/缓存/并发单飞；`loadShard` **同时覆盖两种线上格式**；Worker 不可用时主线程兜底且 `__pksSearchViaWorker` 探针保持为假；`entryShards` 为空时按首字母推导。（依赖 `vi.mock('./contentCache')` + `vi.mock('./searchWorkerClient')` 打断循环依赖。）
- `apps/web/src/lib/searchWorkerClient.test.ts`（8）：能力探测与永久降级（fatal 后**不重建** Worker）、init/shards/search 协议形状与顺序、init 只发一次、探针仅在拿到真实结果后置真、error 只影响单次请求。**该模块持有模块级状态且 `degraded` 永久，每个用例必须 `vi.resetModules()` + 动态 import。**

### 验证
- core **25 文件/181 用例**全绿；web **5 文件/36 用例**全绿；web 与 core typecheck 均通过。
- **变异测试**（证明双格式用例非空洞）：临时移除 `loader.ts` 的旧内联 `index` 回退 → `theory` 用例丢 `alpha`、`graph` 用例返回 `[]`，两条如预期失败；随后 `git checkout --` 还原并 grep 确认回退仍在（第 172 行）。
- `docs/14` Top 8 已由「web 端零测试」改写为现状，并记录两个历史阻塞原因与正确运行命令。

### 未完成
- **P2-12**（OTA 清单签名 + `network_security_config` 收窄）：仅在离开个人局域网时才需要，暂缓。

## 哲学分类扩充（续 · Agent 模式，晚）
- 完成中国哲学缺失条目（各 5 章深度，type/person 或 concept，categories=[哲学/中国哲学]）：
  - `laozi` 老子（5章/≈9500字，含《道德经》文本疑云、道/无为/反者道之动）
  - `moism` 墨家（5章/≈9200字，兼爱/非攻尚贤尚同/天志明鬼三表法/墨辩与中绝）
  - `legalism` 法家（5章/≈9600字，从礼治到法治/商鞅/慎到申不害/韩非/实践评价）
- 全部 `see_also` 指向真实 slug；lint **0 error 0 warn**（node 直调 tsx lint）。
- ⚠ 再现「同文件写静默落错版」：`moism/ch-04` 与 `legalism/ch-01` 初写落为旧/异版，已重写并 Read 回验。
- 清理临时脚本(AUDIT/SURVEY/_out/_wc)+lint 日志；保留 `PHILO_SPEC.md` 作哲学写作规格。
- 提交 `8f7a50f`。哲学分类现状：6 分支总纲(形而上学/认识论/伦理学/美学/政治哲学/自由意志)+人物学派(苏格拉底/孟子/朱熹/斯宾诺莎/洛克/阿奎那/荀子/老子/墨家/法家)+深化(经验/理性主义)。逻辑学已有 formal-logic，中西哲学史主线已由各总纲+人物词条覆盖。
