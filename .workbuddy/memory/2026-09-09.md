# 2026-09-09 工作日志

## 内容录入规划（UI 阶段暂告一段落，转入内容）

用户确认 UI 暂无可改后，转向「内容录入」规划。本轮只做规划 + 决策，**未动代码/未生成内容文件**（用户多次强调"先不急着敲代码"）。

### 用户拍板的三项决策（2026-09-09 00:03）
- **首批范围**：政治理论（马克思主义 / 政治经济学）+ 历史双时间线（中国史/世界史）。
- **内容来源**：原文 + 导读混合（公有领域原文为主，配自写编者注/导读；非纯原创摘要）。
- **首批体量**：小而精 ~15 条 / ~15 万字（快速验证录入管线与阅读体验，再决定是否铺开）。

### 关键澄清（对用户）
- PRD 的「2000 万字」是**终点愿景**（主要=中国政治理论大部头：毛选/邓选等国内公有领域），**不是一次性录入量**，明确不进 B0/B1 全量，B2 起按卷/文件粒度分批。
- 已落地的近期批次（docs/06）：B0 ~20条/~8万字（骨架）、B1 ~100条/~30万字（五线入门路径）。
- 助手**不会虚构**权威知识内容；实质性文字来自公有领域原文导入 + 编者注。

### 录入机制（已就绪，纯 markdown 工作流）
1. 写 `content/entries/<slug>/entry.md`：front-matter(schema:1/slug/title/categories/summary/tags/license/sources/see_also/structure…) + 正文 markdown；多章拆 `chapters/*.md`。
2. 新类目改 `content/taxonomy.yaml`（零代码扩展，空节点不崩）。
3. 学习序列 `content/tracks/*.yaml`（中国史/世界史双线已建，cross_timeline）。
4. 管线：`npm run build:index` → `npm run copy:content`（`.index`→`index`）→ `npm run build` → `cap sync android` → assembleDebug；`npm run lint` 校验 24+ 规则。
   - 注意：core 用手工 junction 链接，勿 `pnpm`/`npm install`；改了 packages/core/src 须先重编 core dist。

### 首批 ~15 条候选清单（政论+历史）
现有 10 条多为节选/桩，优先补完：
- 政论：共产党宣言(补整本)、资本论(卷一选读，桩→可读)、剩余价值理论(桩→扩)、劳动价值论(桩→扩)、利息率(桩)、德意志意识形态(费尔巴哈章)、反杜林论(引论)、矛盾论、实践论
- 历史：鸦片战争(桩→扩,中史)、五四运动(桩→扩,中史)、工业革命(桩→扩,世史)、法国大革命(世史)、十月革命(世史/政论交叉)、改革开放(中史近现)
- 注：cognitive-bias 属「科学/认知」，不在本批范围，保持现状。

### 下一步（待用户"开始"）
- 提议先以《共产党宣言》整本补录作为**管线验证首件**（已有节选桩，公有领域，扩成全文+导读），跑通 build:index→copy:content→build→cap sync 后由用户真机/桌面复验，再批量铺开其余 14 条。
- 用户尚未说"开始"，按其一贯节奏**先对齐方案、再执行**。

## 会话暂停（2026-09-09 00:10）
- 用户结束本轮：**关掉后台 dev server（task dbKvlN，已 kill，运行 25min）**，约定 **10 小时后**继续。
- 已交付：① 章节进度条卡 100% 的修复（portal 到 body，typecheck 通过）② 内容录入规划（三项决策 + 15 条清单 + 管线）。
- 重启后第一件事：确认《共产党宣言》补录是否开工；若开工，先跑通 build:index→copy:content→build→cap sync 验证管线。
- 记忆：本会话无活跃子代理（内容规划为纯规划，未派 worker），无需 TeamDelete。

---

## APK 打包（用户要在手机上看）

- 完整构建链成功：`build:index` → `copy:content` → `vite build` → `cap sync android` → `gradlew assembleDebug --no-daemon`。
- 关键环境：JAVA_HOME=`D:/JDK/jdk-19.0.1`，ANDROID_HOME=`D:/Android SDK`，CODEBUDDY_SAFE_DELETE_ENABLED=0。
- 直接调用 managed node 跑 vite/cap（不走 npm，避免 pnpm/workspace 协议问题）：`$NODE node_modules/vite/bin/vite.js build`、`$NODE node_modules/@capacitor/cli/bin/capacitor sync android`。
- 产物：`apps/web/android/app/build/outputs/apk/debug/app-debug.apk`（约 4.2MB，BUILD SUCCESSFUL）。
- 已通过 present_files 交给用户下载到手机内测。debug 包，非正式签名。

---

## 手机端 UI 修两处（用户截屏反馈）

- 搜索结果章节前缀 `§` 在 Android WebView 乱码 → `SearchPage.tsx:115` 改为 `·`（全角中点）。
- 阅读页目录（`EntryReaderPage.tsx` 章节浮层）：
  - 删除 `字数 · 评论数 暂无` → 改为显示真实每章字数 `${words.toLocaleString()} 字`（`DocDef` 增加 `words`，来自 `flattenChapterList` 的 `c.words`；导读「序」words=0 不显示）。
  - 整行可点击：原 `<button class=chapter-row-main>` 改为外层 `div.chapter-row` 带 `role=button/tabIndex/onClick/onKeyDown`，内层改为 div（修复「只有标题文字可点」）。
  - CSS `.chapter-row` 加 `cursor:pointer`、`:active`/`:focus-visible` 反馈。
- 涉及文件：`SearchPage.tsx`、`EntryReaderPage.tsx`、`styles.css:3511`。
- typecheck 通过；重打包 APK（task cvsF0D，BUILD SUCCESSFUL）→ `app-debug.apk`（约 4.2MB）。

---

## 返回过场 + 目录二级小节（用户手机反馈）

### 返回过场 / 导航栏
- 根因：阅读页「返回」用 `navigate`（push 历史层）→ 系统返回又弹回阅读页，且顶栏不出现。改为 `replaceRoute`（原地替换），与 `backHandler` 一致。文件 `EntryReaderPage.tsx:235`、`router.tsx`。
- `AppShell.tsx` 加 effect：离开阅读态(reset `isReader`)时 `setHeaderHidden(false)` + `lastYRef=0`，保证「知识」导航栏必现。
- 过场动画（`styles.css` 末尾）：`.route-fade` 路由容器 `routeIn` 淡入上移；`.app-header/.app-footer/.app-tabbar` 加 `chromeFade` 仅 opacity 淡入（避免与隐藏动画 transform 冲突）；`App.tsx` 路由 div 加 `className="route-fade"`。

### 目录二级小节
- 新增 `fetchDocHeadings(slug, doc, knownSlugs)`（`lib/content.ts`）：抓 main/章节 markdown 标题，main 取 level>=2、chapter 取 level>=3（排除章节 H2 标题本身）。用 `parseYamlFrontmatter` 取 body（无 `splitFrontmatter`，已修正）。
- `EntryReaderPage.tsx`：装载 `headingsMap`（各文档标题）；目录每章下以缩进 `.chapter-sublist` 显示小节；点章节 setIndex 跳章，点小节 setIndex + `pendingScroll` 载入后滚动到该标题（`.prose h2/h3/h4` 文本匹配）。
- CSS：`.chapter-group` 承载 border（替换原 `.chapter-row` border）；`.chapter-sublist`/`.chapter-subitem` 缩进样式；`.chapter-group.is-active .chapter-row` 高亮。
- typecheck 通过；重打包 APK（task jmfNIM，BUILD SUCCESSFUL）。

---

## 搜索结果精简（@1.jpg 反馈）

- `SearchPage.tsx` 全文结果段：移除逐章展开的 `result-hits`（每词条第 5 条命中小节 + 分数），只保留词条卡片（标题 + `N 处命中 · W 字`）。避免数据增多后搜索列表过长。
- 此前为 `§ `/`· ` 前缀的小节标题也随列表一并移除。
- 后台 dev server 任务 `1DrXMt` 已 TaskStop 结束。
- typecheck 通过；重打包 APK（task D7i0yh，BUILD SUCCESSFUL）→ `app-debug.apk`。

---

## 批量补录开工（15 条计划，14 条待补）

《共产党宣言》已作为管线验证首件完成。剩余 14 条并行分派给 6 个 general-purpose worker（各写各的目录，无冲突）：
1. `surplus-value`, `labour-theory-of-value`
2. `interest-rate`, `das-kapital`（后者已有 chapters/v1-p1-ch01/02，扩写并补章）
3. `german-ideology`(新), `anti-duhring`(新)
4. `on-contradiction`(新), `on-practice`(新) —— 用 CC-BY-SA-4.0，以概括阐释为主
5. `opium-war`, `may-fourth-movement`, `reform-and-opening`(新)
6. `industrial-revolution`, `french-revolution`(新), `october-revolution`(新)

### 给 worker 的统一硬约束（复用模板）
- 只写 `content/entries/<slug>/entry.md` + `chapters/*.md`；不改 taxonomy.yaml / apps / packages；不跑任何构建命令（由 lead 统一跑）。
- 章节 key = 文件名去 .md，slug = `<词条slug>/<key>`，work = 词条 slug；章节文件名零填充数字前缀保证排序。
- **tldr ≤110 中文字符**（lint 硬限 120）。
- see_also 只能引用 17 个合法 slug（现有 10 + 本批新建 7）。
- categories 只能用 taxonomy 里已存在的路径（本批 7 条全部命中现有类目，无需改 taxonomy.yaml）。
- 严禁编造史实/引文/日期；拿不准不写或标「编者注：此处说法不一」。
- 体量目标 2500–4000 汉字/条。

### 待 worker 回报后
build:index → lint（重点查 L004 tldr 超长 / L006 see_also 悬空）→ copy:content → vite build → cap sync → assembleDebug → 交付 APK。

---

## 批量补录 第一批结果（遭遇 429 限流）

### 失败情况
6 个 worker 全部返回 **429 用量频率限制**（重置时间 2026-09-09 16:56:14 UTC+8）。但多数在限流前已把文件写完，实际落盘如下：

已完成 7 条（含新建/扩写）：
- surplus-value 4144 字(5章)、labour-theory-of-value 3719(5章)、interest-rate 3144(5章)
- german-ideology 4436(5章,新)、on-contradiction 4791(7章,新)、on-practice 3879(6章,新)
- opium-war 3158(6章)

半成品 1 条：das-kapital（entry.md 已扩写，但章节仍只有原 2 章，未补新章）
未动 6 条：anti-duhring(新)、may-fourth-movement(扩)、reform-and-opening(新)、industrial-revolution(扩)、french-revolution(新)、october-revolution(新)

### 修复（lead 亲自做，未替 worker 虚构内容）
- `labour-theory-of-value/chapters/ch-05.md` YAML 解析失败：keyPoints 里 `- "转形问题"...` 以 ASCII 双引号开头被当成带引号标量 → 改为中文弯引号 “转形问题”。
- 移除 4 处悬空 see_also（anti-duhring、reform-and-opening 尚未创建）：on-contradiction、on-practice、opium-war。
  → 这两条新词条建好后需把引用加回去。

### 结果
build:index：13 词条 / 45 章节 / **40051 字**（原 16583）；lint **0 error 0 warn**。
重打包 APK（task dvkEvu）。

### 待办（限流解除后）
补 6 条缺失 + 补齐 das-kapital 章节 + 把 anti-duhring / reform-and-opening 加回相关 see_also。

---

## 视觉打磨 + 内容补录完成（主线程自写，绕开 worker 429）

用户要求：①按"稳的一版"做移动端视觉 CSS 打磨；②补完限流未完成的内容。

### CSS 打磨（styles.css 末尾新增「M3 移动端视觉打磨」块，纯 CSS 不进依赖）
- html/body 及可点元素加 `-webkit-tap-highlight-color: transparent`；`touch-action: manipulation`。
- 可点元素按压微缩放：`.btn:active`、`.chip-link`、`.result-card-link`、`.cover-chapter-row`、`.tab-item` 等 `scale(.96-.97)`。
- 触控目标：`.cover-chapter-row`、`.chapter-row` min-height 44px。
- 卡片 hover 轻反馈（translateY(-2px)）。
- `@media (prefers-reduced-motion: reduce)` 关闭上述非必要动画。

### 内容补录（16 词条全部落盘，lint 0/0）
- 因 worker 全线 429（配额到 16:56 才重置），剩余内容全部由主线程亲自撰写（主线程不受限流）：
  - 已完成（本轮新增）：das-kapital 补 v1-p1-ch03/ch04(第二篇)/ch05(第三篇)（现5章，修 entry 尾注）；french-revolution（entry+6章）；october-revolution（entry+6章）；reform-and-opening（entry+6章）；anti-duhring（entry+5章）；may-fourth-movement（扩写 entry+5章）；industrial-revolution（补6章）。
  - 又把 anti-duhring/reform-and-opening 加回 on-contradiction/on-practice/opium-war 的 see_also。
- **YAML 教训：keyPoints 项若以 ASCII 双引号 `"` 开头会触发 js-yaml 解析失败**；以中文弯引号 `“` 开头无碍，但最稳妥是不以引号开头（曾两处踩坑：labour-theory ch-05、reform ch-01）。
- build:index：**17 词条 / 82 章节 / 64355 字**（原 40051），lint 0 error 0 warn。15 条计划词条全部就位（另含 cognitive-bias/dialectics 两条原有未在计划内）。

### 待打包
重打包 APK（task 4Kaw4n）。

---

## 暖色柔和换肤 + 补完两个 0 章节桩词条（lead 主线程完成）

用户反馈：「整体 UI 没有什么改变」+「继续补充整体内容的录入」。此前打磨仅是手感层（去点按高亮/按压缩放），非视觉大改。

### UI：整套暖色柔和换肤（styles.css，肉眼可见）
- `:root` token 冷蓝灰 → 暖色系：`--bg:#faf6f0`、`--surface:#fff`、`--border:#ece3d6`、`--text:#261f16`、**brand=`#cf5f2b`（陶土橙）**、brand-strong=`#b74e1e`、brand-soft=`#fbe7d6`、accent=苔绿`#5b8a4e`、radius 22/14、新增 `--surface-2`、`--radius-pill`。
- 首页「时间线卡」改为主打卡片：暖橙渐变 + 右上光晕伪元素 + 白色文字。
- 底部标签栏选中态改**暖色药丸高亮**（is-active 加 brand-soft 底 + brand 字），active 按压缩放；dark 模式补 `rgba(245,178,129,.14)` 底。
- `.brand-mark` logo 改暖橙渐变；`.timeline-card-arrow` 白色。
- 清除了全部**残留冷蓝**：`rgba(47,109,246,…)`×3（tl hover/搜索 focus ring/章节阴影）、`#1c4fbb`、`#0b7d5f`、`#2f6df6` 等 → 暖橙/暖绿对应；`.reader-sheet-bg-white` accent 改 `#cf5f2b`；dark active tab `#f0975f`。
- **教训：dist CSS 里残留冷蓝往往来自 `rgba(47,109,246,α)` 这种带 alpha 的写法**，grep 只搜 hex `#2f6df6` 会漏——校验换肤完整性要一并搜 `rgba(47,109,246`。
- 仅动 CSS，结构零改动；typecheck 不需（无 TS）。已多次重打包验证 `BUILD SUCCESSFUL`。

### 内容：补完最后 2 个 0 章节桩词条
- `dialectics`（辩证法，哲学/思想史）：原 698 字符桩 → **entry 导读 + 5 章**（源流与黑格尔体系/对立统一/量变质变/否定之否定/应用与当代意义），entry 加 structure 块 + see_also 增 anti-duhring/on-contradiction/cognitive-bias。
- `cognitive-bias`（认知偏差，科学/人类认知与心理）：原 681 字符桩 → **entry 导读 + 5 章**（何谓认知偏差/判断类/决策与社会类/记忆类与典型现象/意义与应对），see_also=[dialectics,interest-rate]。
- YAML 老坑再现：keyPoints 项**不能以 ASCII 引号 `"` 开头**（ch-01 `"辩证法"` 开头触发 parse 失败）→ 改写为不以引号开头。
- lint 0/0；build:index：**17 词条 / 92 章节 / 71637 字**（原 64355）。

### 待办
- 用户要「大幅扩展 + 名著补全文 + 扩类目 + 铺数量」是大工程，需分批。本轮仅补完 2 桩；后续批次待用户对换肤 APK 反馈后再定方向与优先级。

---

## 内容推进：das-kapital 卷一补全（用户认可 UI 后）

用户认可暖色 UI，要求"继续推进内容"。按既定"先名著补全文"方向，本轮补 **das-kapital（资本论）卷一脉络 5→8 章**：
- 原 5 章只覆盖 第一/二/三篇；本轮补 3 大缺失篇块：
  - v1-p4-ch06 相对剩余价值生产（协作/分工/工场手工业/机器大工业），order [1,4,6]
  - v1-p6-ch07 工资（劳动力价值转化形式/计时计件工资），order [1,6,7]
  - v1-p7-ch08 资本的积累过程（简单/扩大再生产、有机构成、相对过剩人口、原始积累），order [1,7,8]
- entry.md 更新：rev 4、total_sections 8、total_words 9200、末段「地位」范围描述改写。
- **章节命名约定**：das-kapital 用 `v1-pX-chYY.md`（key 同），`order:[卷,篇,序]`、`path:[卷一 资本的生产过程, 第X篇 ..., 第YY章 ...]`、depth:1、license public-domain、sources ref entry://das-kapital。
- lint 0/0；build:index：**17 词条 / 95 章节 / 74890 字**（原 71637）。

### 后续扩展方向（待用户反馈后推进）
- 其他大部头名著补脉络：communist-manifesto(4部)、german-ideology、anti-duhring 等。
- 薄类目铺数量：科学/人类认知与心理(仅 cognitive-bias 1条)、经济学/金融(仅概念)、哲学/思想史偏薄。
- 历史类缺「系统脉络」词条（现为单点事件）。

---

## 大工程启动：三大方向全做 + App 图标重做

用户拍板"三大方向全都要，当大工程做"+ "App 图标也要改"。

### App 图标重做（已完成）
- 旧图标 = 金色方块 + 白色「知」字，与暖色 UI 不搭。
- 设计：新图标 = **暖陶土橙渐变** + **奶白"展开的书"抽象图形** + 书脊顶部暖光小圆，匹配 brand 渐变 `#e07a36 → #cf5f2b → #b74e1e`。
- 生成器：`apps/web/scripts/gen-icons.py`（Pillow），输出：
  - Android `mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png`、`ic_launcher_round.png`、`ic_launcher_foreground.png`（前景 432px@xxxhdpi 留 66dp 安全区）。
  - Web `public/favicon.ico` + `favicon-32/192/512.png` + `apple-touch-icon.png`。
- `ic_launcher_background` 色从 `#C9A227` 改 `#cf5f2b`；`index.html` 加 favicon links。

### Pillow 安装（managed python venv）
- 路径 `C:/Users/Yu/.workbuddy/binaries/python/envs/default`（用 managed 3.13.12 创建）。
- 装 `pillow 12.3.0`，后续做图标的脚本可直接复用。

### 内容新增（本轮）
- 新建 `heuristics`（启发式，科学/人类认知与心理）：5 章 + 导读，与 cognitive-bias 互参。
- 新建 `inflation`（通货膨胀，经济学/金融）：5 章 + 导读，与 interest-rate 互参。
- 见_also 关联：cognitive-bias +heuristics、interest-rate +inflation。
- **lint 经验：summary 用 `>-` 折叠标量时，字符数按**折叠后的纯字符串**计（去换行、去前导空格），触发 L008 "≥80字"硬限；当前 ~68-70 字踩线 → 加字到 ≥80 即可。

---

## 阅读页「可增量」批（用户从大方案里选定，纯 CSS/无引库）
用户给了超长阅读页重构方案，我逐项评审后发现**约一半已在现有代码实现**（沉浸点唤、字号/字体/行距/页边距/亮度/5种翻页动画/5主题色板/夜间/进度条跳章/safe-area）。真正值得增量的 4 项，用户拍板先做这批、仿真翻页留后评估。已全部实现 + typecheck 通过：

1. **自动加载下一章**（EntryReaderPage.tsx 滚动 effect 改造）：
   - 仅在「内容可滚动 max>0 且 scrollTop≥max-4」触发（短章不级联跳），去抖 400ms 防惯性。
   - 用 `totalRef`(docs.length) + `setIndex(cur=>cur+1 if <total-1)`，触发后走既有 loading spinner。
2. **信息栏滚隐**：overlay 唤出态下滚动 → `.chrome-dismissed` 隐藏上下栏（transform±6px 淡出），停顿 1.8s 自动淡入；仍保留点中央唤出。ref: `chromeRevealTimer`、state `chromeDismissed`、CSS `.reader-root.chrome-dismissed .reader-top/.bottom`。
3. **两端/左对齐**：新增阅读偏好 `align`(justify|left)，`preferences.ts` 全链路（type/key/default/READ_KEYS/ALIGNS/read/apply 写 `body[data-reading-align]`）；ReaderSettingsSheet 加「对齐」2 胶囊行；CSS `body[data-reading-align='left'] .prose p{text-align:left}`。
4. **目录抽屉搜索**：chapter sheet 加 `.toc-search` 搜索框（state `tocQuery`），按章节名/二级小节过滤；命中小节才展开该组，无结果显示空态 `.toc-search-empty`；点击定位逻辑不变（pendingScroll）。CSS `.toc-search/.toc-search-list/.toc-search-empty`。

### 实现注意
- `docs` useMemo 声明在顶部区域之后，`totalRef.current=docs.length` 若放其前会 TS2448「used before declaration」→ 必须放 useMemo 之后。
- 该批次全部逻辑在 EntryReaderPage.tsx + preferences.ts + ReaderSettingsSheet.tsx + styles.css，零新依赖。
- typecheck 0 error；重打包（task Jdk9Cx）。

---

## 自动加载做成开关 + 内容继续铺（本轮不交付，攒下次）

### 阅读页：「滚动到底自动翻章」做成开关
- 用户要求把上一批做的「自动加载下一章」收进「更多」开关，默认开，可关。
- 实现：ReaderPrefs 增 `autoLoad: boolean`（default true，key `pks_pref_read_autoload`）。
  - preferences.ts 全链路：type/READ_KEYS/DEFAULT/read（新增 helper `readBoolWithDefault(raw,fallback)`，'1'/'true'→true，null→fallback）/无需 apply 到 DOM（reader 用 ref 读）。
  - ReaderMoreSheet：MORE_TOGGLE_KEYS 增 'autoload'，label「滚动到底自动翻章」；toggle() 中 autoload 与 statusbar 一样走 `onChange('autoLoad'|'statusbarPermanent', next)`，其余走 localStorage flag。
  - EntryReaderPage：新增 `autoLoadRef`（prefs.autoLoad 每帧刷新）；滚动 effect 里 auto-next 触发条件加 `autoLoadRef.current`，关闭时也清掉去抖计时器。
- 关键经验：**能进 ReaderPrefs 的统一进 ReaderPrefs**（走 writeReadPref/onChange/ref 读取），不要混用 ReaderMoreSheet 的独立 localStorage flag，否则跨组件状态不同步。
- typecheck 0 error；**未打包**（用户要攒到下次一起交付）。

### 内容再铺 2 条（Batch B 续）
- `mental-model`（心智模型，科学/人类认知与心理）：导读+5 章（概念来源/如何运作/跨领域典型模型/局限失真/如何建立更新）。
- `exchange-rate`（汇率，经济学/金融）：导读+5 章（概念标价/决定机制/汇率制度/经济影响/人民币汇率改革）。
- see_also 关联：cognitive-bias/heuristics +mental-model；interest-rate/inflation +exchange-rate。
- lint 0/0；build:index：**21 词条 / 115 章节 / 88840 字**（原 19/105/82034）。
- 待交付：本轮 + 上轮的 UI/内容改动全部攒着，下次一起打 APK 交付。

---

## 内容大推进：taxonomy 扩骨架 + 单轮 5 新词条

### taxonomy.yaml 扩充为完整领域骨架（保留既有 node id/title）
- 背景：taxonomy 原来极粗（5 L1 仅少量 L2/L3），词条偏科（近代史+马经典+认知心理+少量金融）。
- 关键：**词条 categories 用「标题路径」引用**（如 科学/人类认知与心理），故新增节点必须保留既有节点 id+title，只加兄弟/子节点，否则旧 21 词条解析失败。
- 扩展结构（节选）：history 世界史下分 古代/中世纪/近代/现当代，中国史分 先秦秦汉/魏晋隋唐/宋元明清/近现代；philosophy 增 中国哲学/西方哲学(古希腊/近代欧陆/英美现当代)/逻辑与批判性思维；science 增 科学方法论/自然科学基础(物理化学化生物天文)/数学与系统；economics 增 微观/宏观/金融(货币银行/资本市场)/学说流派；politics 增 政治思想/政治学概念/政体制度。
- 空节点 UI 正常：BrowsePage 对空类目显示「该类目暂无已发布词条」，不崩。
- 校验：lint 0/0（会校验 category 能否解析）。

### 单轮 5 条新词条（各导读+5 章，跨 4 个 L1）
1. `confucianism` 儒家思想（哲学/中国哲学）：兴起背景/仁礼义/修身五伦/政治社会构想/历史流变。
2. `scientific-method` 科学方法论（科学/科学方法论）：概念/观察假说实验/归纳演绎可检验/证伪与范式/边界今日。
3. `law-of-supply-demand` 供需法则（经济学/微观经济学）：需求/供给/均衡价格/弹性/边界批评。
4. `money` 货币（经济学/金融/货币与银行）：物物交换到一般等价物/职能/形态演化/价值与货币创造/宏观。
5. `renaissance` 文艺复兴（历史/世界史+哲学/思想史）：背景与意大利/人文主义/艺术/思想扩散/边界意义。
- see_also 补反向链接：mental-model+科学方法论；interest-rate/inflation/exchange-rate +供需 +money。
- lint 0/0；build:index：**26 词条 / 140 章节 / 103123 字**（21→26 跨越 10 万字）。
- 经验：**单轮量产新词条后务必 lint+build:index 双验**；写满 5 章很耗时，量力分批，宁可每轮 3-5 条质量稳，勿图快凑数。

---

## 内容续铺（批次B/C）：主线程直接写 + 委派 worker 被 429 拦

### 委派并行被频率限制拦
- 尝试并行拉起 4 个 general-purpose worker 各写 3 条，全部 **429「使用量超出频率限制，2026-09-09 16:56:14 UTC+8 重置」**。团队模式被限流挡住，只能退回主线程逐条写。
- 教训：429 期间别开多 worker 撞墙；主线程单条顺产即可（本轮主线程仍稳定产出 4 条）。

### 主线程新增 4 词条（各导读+5 章，跨 历史/哲学）
1. `daoism` 道家思想（哲学/中国哲学）：老庄/道无自然/无为养生/与儒法分野/影响。
2. `ancient-greek-philosophy` 古希腊哲学（哲学/西方哲学/古希腊哲学）：前苏/苏格拉底/柏拉图/亚里士多德/希腊化。
3. `enlightenment` 启蒙运动（历史/世界史/近代史+哲学/西方哲学/近代欧陆哲学）：背景/理性权利契约/英法思想家/传播与争议/影响批判。
4. `qin-han-unification` 秦汉大一统（历史/中国史/先秦至秦汉）：秦并六国制度/暴政速亡/汉承秦制文景/汉武帝/两汉兴衰定型。
- see_also 反向补：confucianism+daoism；renaissance/french-revolution+enlightenment；opium-war/may-fourth-movement+qin-han-unification。
- lint 0/0；build:index：**30 词条 / 160 章节 / 116071 字**（26→30 跨 11.6 万字）。累计本轮单主线程 4 条成功。
- 注意：qin-han 编者注以纯文本提到"科举制度/隋唐盛世"（planned 未建），只要不进 see_also 就不触发 lint，安全。

### 待交付清单（下轮一起打 APK）
- 阅读 UI：暖橙图标、5 项阅读增量（自动翻章开关=更多里）、等——仍需验证。
- 内容：已到 30 词条。批次 B/C/D 剩余：sui-tang(隋唐)、imperial-exam(科举)、roman-empire、age-of-discovery、utilitarianism、evolution-theory、fiscal-policy、separation-of-powers 等。

---

## 内容续铺（主线程，用户选"继续主线程"）：单轮再 +7 条，30→37

用户明确「继续主线程」→ 全程主线程逐条写，未再试并行（429 重置时间未知）。

### 主线程新增 7 词条（各导读+5 章）
- 批次B 中国史：`sui-tang` 隋唐盛世（历史/中国史/魏晋至隋唐）；`imperial-exam` 科举制度（历史/中国史/宋元明清+政治理论/政体与国家制度）。
- 批次D 科学/经济/政治：`evolution-theory` 进化论（科学/自然科学基础/生物与生命）；`fiscal-policy` 财政政策（经济学/宏观经济学）；`utilitarianism` 功利主义（哲学/西方哲学/英美与现当代哲学）；`separation-of-powers` 分权制衡（政治理论/政治学概念）。
- 批次B 世界史：`roman-empire` 罗马帝国（历史/世界史/古代文明）。
- see_also 反向补：scientific-method+evolution-theory；money/fiscal-policy；enlightenment+separation-of-powers；law-of-supply-demand+utilitarianism；renaissance/ancient-greek-philosophy/qin-han+roman-empire 等。

### 关键 YAML 坑（已入 MEMORY.md 长期项）
- **frontmatter 列表项绝不能用引号开头**：`- "宁做…"` 或 `- “转形问题”…` 都触发 `bad indentation of a sequence entry` parse 失败。去掉开头引号即可（引号在句中无碍）。修了 3 处（labour-theory ch05、utilitarianism ch01/ch02）。
- 教训：批量写内容后跑 lint 会一次性暴露这类 parse 错，逐个删开头引号即解。

### 状态
lint 0/0；build:index：**37 词条 / 195 章节 / 140148 字**（30→37）。累计本轮 +7。
- 跨 10 万字后稳定推进；批次 B(世界/中国史) 已较实，D(科学/经济/政治) 也开始填充。
- 剩余仍空：world 中世纪、china 宋元明清断代(除科举)、哲学 逻辑/道家已补/心学未、科学 物理化学数学未、经济 资本市场/流派未、政治 政治思想未等，可继续下轮铺。

---

## 交付当前版 APK（37 词条 + 阅读页增量）
- 用户结束积累期，要求先交付当前版。
- 全链路一次过：build:index(37/195/140148字) → copy:content(37词条) → vite(363模块 14.7s) → cap sync → gradle **BUILD SUCCESSFUL 46s** → ALL DONE exit=0。
- APK：`apps/web/android/app/build/outputs/apk/debug/app-debug.apk`，5.1MB，16:24 产出。
- 验证：dist JS 含 autoLoad/readingAlign/toc-search（阅读页 5 项增量在）；dist/content/entries=37 词条在。
- 交付内容：37 词条内容 + 阅读页 5 项增量（滚到章末自动加载【更多里开关】、信息栏滚隐、两端/左对齐切换、目录搜索）。
- 此 APK 未含：UI 暖橙图标（若曾改过需确认是否并入——本轮 build 直接基于当前源码，图标若为源码内资源则已含）。

---

## 长时程内容推进（用户要求持续到2小时后）：完善时间线 + 43词条

### 时间线（tracks）完善 —— 关键机制
- 结构：`content/tracks/history-china.yaml`（中国主线）+ `history-world.yaml`（世界主线），`order_mode: chronological`，item 含 order/entry/sort_date/date_label/era/note，可加 `cross_timeline: true`。
- **词条 frontmatter 需带 `timeline:` 字段**（如 `timeline: [china]`/`[world]`/cross=`[china, world]`），lint L005：cross_timeline 条目其词条 timeline 维度必须 ≥2，否则 warn。
- 已扩充：中国线 = qin-han(前221)/sui-tang(581)/imperial-exam(605)/opium-war(1840 cross)/may-fourth(1919)/reform-and-opening(1978)；世界线 = roman-empire(前27)/renaissance(1300)/age-of-discovery(1492)/enlightenment(1700)/industrial-revolution/french-revolution/opium-war(cross)/october-revolution。覆盖 古代→现当代。
- 给被引入词条 entry 补 timeline 字段（置于 tags 与 summary 之间）。

### 长时程批量新增词条（各导读+5章）
批次：`age-of-discovery` 大航海时代(世界史中世纪,world线)、`existentialism` 存在主义(哲学/西方现当代)、`classical-mechanics` 经典力学(科学/物理)、`stock-market` 股票市场(经济/资本市场)、`keynesian-economics` 凯恩斯主义(经济/流派)、`sovereignty` 主权(政治/政治学概念)。均补 see_also 反向链。
lint 0/0；build:index：**43 词条 / 225 章节 / 161556 字**（37→43 跨 16 万字）。
- 策略：每批 6 条，写完 lint+build 双验；带时代/制度的历史词条挂对应时间线。可持续多批推进。

批次2：`scientific-revolution` 科学革命(历史世界近代,入 world 线)、`critical-thinking` 批判性思维(哲学/逻辑)、`memory` 记忆(科学/认知过程)、`emotion` 情绪(科学/心理分支)、`adam-smith` 亚当斯密(经济/流派)、`democracy` 民主(政治/政体)。scientific-revolution 已插入 history-world.yaml(order 4, 1543)。补认知簇反向链。build:index：**49 词条 / 255 章节 / 189549 字**（43→49）。

批次3：`capitalism` 资本主义(经济/流派+马政经)、`nationalism` 民族主义(政治/政治思想)、`cold-war` 冷战(世界史/现当代,入 world 线)、`globalization` 全球化(世界史/现当代+宏观,入 world 线)、`periodic-table` 元素周期表(科学/化学)、`complexity` 复杂系统(科学/系统与复杂性)。cold-war(1947)/globalization(1990) 已插 world 时间线。build:index：**55 词条 / 285 章节 / 233070 字**（49→55）。

批次4：`silk-road` 丝绸之路(中国史先秦秦汉+世界史中世纪,**入 china+world 双线 cross**,前138 张骞凿空)、`astronomy` 天文学(科学/天文)、`probability` 概率论(科学/数学基础)、`formal-logic` 形式逻辑(哲学/逻辑)、`central-banking` 中央银行(经济/货币银行)、`rule-of-law` 法治(政治/政治学概念)。注意世界时间线插入 silk-road 后需把后面全部 order 顺延重排(1→2→3…)。build:index：**61 词条 / 315 章节 / 279581 字**（55→61）。

批次5：`buddhism` 佛教(哲学/中国哲学)、`chinese-marxism` 马克思主义中国化(马经典+中国近代史)、`republic` 共和政体(政治/政体)。补 see_also 反向链(daoism/confucianism/silk-road→buddhism；democracy/sovereignty→republic)。build:index：**64 词条 / 330 章节 / 303702 字**（61→64,首破 30 万字）。

批次6（时间线大补）：世界史入 world 线 `ancient-egypt`(古埃及,前3100 首位)、`first-world-war`(一战1914,注意排在十月革命之前)、`crusades`(十字军1096 中世纪,插罗马与文艺复兴之间)——故 world 线已 15 节点：古埃及→丝路(cross)→罗马→十字军→文艺复兴→大航海→科学革命→启蒙→工业→法国大革命→鸦片(cross)→一战→十月→冷战→全球化。中国史入 china 线 `song-dynasty`(宋960)、`ming-dynasty`(明1368)、`qing-dynasty`(清1636)（皆 历史/中国史/宋元明清, timeline:[china]）→ china 线 10 节点：秦汉→丝路(cross)→隋唐→科举→宋→明→清→鸦片(cross)→五四→改革。注意 Write 工具参数顺序坑：file_path/content 顺序颠倒会报 Invalid parameters，须 file_path 在前。build:index：**70 词条 / 360 章节 / 338059 字**（64→70）。

批次7（末空叶+深化）：`historiography` 史学方法(**历史/历史方法与概念——最后一个空叶子填满，至此 taxonomy 全叶子均有词条**)、`mind-learning` 阳明心学(哲学/中国哲学)、`game-theory` 博弈论(经济/微观)、`thermodynamics` 热力学(科学/物理)、`nietzsche` 尼采(哲学/近代欧陆,note: 本批 entry see_also 曾误写 `utilitarian-ism` 已改回 `utilitarianism`)。补反向链(confucianism/buddhism→mind-learning；existentialism→nietzsche；law-of-supply→game-theory；classical-mechanics/complexity→thermodynamics)。build:index：**75 词条 / 385 章节 / 362780 字**（70→75）。

批次8：`monetary-policy` 货币政策(经济/宏观)、`quantum-mechanics` 量子力学(科学/物理)、`liberalism` 自由主义(政治/政治思想,学术中性表述)、`anti-japanese-war` 抗日战争(中国史/近现代,**入 china 线 1937**,插五四1919与改革1978之间,改 reform 为 order 11)、`stoicism` 斯多葛学派(哲学/古希腊)。补反向链(money/classical-mechanics/ancient-greek/nationalism/opium/qing/may-fourth 等)。build:index：**80 词条 / 410 章节 / 388027 字**（75→80）。china 线 11 节点、world 线 15 节点。

批次9：`chaos-theory` 混沌理论(科学/系统与复杂性)、`market-failure` 市场失灵(经济/微观)、`psychology` 心理学(科学/心理学分支,侧重学科史)、`social-contract` 社会契约论(政治/政治思想,霍布斯洛克卢梭罗尔斯,学术中性)。补反向链(complexity/law-of-supply/mental-model/emotion/enlightenment/sovereignty)。build:index：**84 词条 / 430 章节 / 410161 字**（80→84,首破 41 万字 / 430 章）。

交付（84词条版 APK）：19:20 全链路一次通过(build:index 84词条 → copy → vite 363模块 → cap sync → gradle 1m44s BUILD SUCCESSFUL)。APK 6.88MB @ apps/web/android/app/build/outputs/apk/debug/app-debug.apk。验证：84 词条入 bundle、双时间线编译进 dist/content/index/tracks.json(26 节点=china11+world15 全含新词条)、阅读页增量在 JS。日志 build-apk-84.log。

## UI 优化方案评审（存档，未改代码）
- 用户带来一份外部 AI 生成的《知识阅读类 App UI 优化方案》，要求评估可行性（明确"先不急着敲代码"）。
- **关键澄清**：截图确认是本项目，但为**旧版本**——方案描述的"纯列表时间轴/无按压反馈/古板灰白"在旧版成立、当前版本已全部改进；而方案写的 MUI6+Tailwind3+ReactRouter **本仓库从未安装过**（package.json+无配置文件+src grep 0 命中三证）。
- **评审结论：6 条中 5 条否决/改造，仅"列表交错入场"值得做。**
  - 否决：framer-motion（红线A：@pks/core 靠手工 symlink，install 必断链；且无 `<Outlet/>` 令 AnimatePresence 无处挂载）、MUI createTheme、tailwind.config、Google Fonts（红线B：无 INTERNET 权限，05 §6 Q23）、底栏默认毛玻璃（现为 .96 不透明，blur 增益≈0 却常驻合成层）、时间轴重做（已实现）。
  - P0（纯 CSS 零依赖）：① 列表 stagger `style={{'--i':i}}` + `animation-delay: min(calc(var(--i)*40ms),320ms)`，须尊重 `data-sys-anim=none` 与 prefers-reduced-motion；② `.card-hover`/`.track-card-link`/`.tl-item` 补 `:active` scale（移动端无 hover=触摸零反馈）；③ `.tl-item.is-cross .tl-rail::before` 竖线染紫（圆点已紫、竖线仍灰，双轨断开）。
- 产出存档：`docs/09-外部UI优化方案可行性评审.md`。
- 教训：外部 AI 未读代码生成的方案，"手段层"必然脱节，须先三证技术栈；且**用当前版本截图，勿用旧版**。

## 首页类目导航改造（已派工，待回报）
- 用户用 03_分类导航.jpg 反馈"类目多了观感不好"，要求按市面 App 给出方案，**整合 P0 三条**。
- 给了 1 个推荐方案（卡片 + 折叠树），用 show_widget 渲染了 340px 移动端预览。
- 用户三连拍板：①采纳大方向 ②L3 就地展开 ③暂不加"最近浏览"。
- 派 software-engineer-web 实施，规格已写完整（含 L1 颜色映射、stat-strip/timeline-card 重写、P0 三条、build 验证）。等待回报。

## 首页类目导航改造（已交付）
- engineer-web 回报完成：Home.tsx 57→262 行，styles.css 末尾 V5 区块（约 240 行）。
- L1 彩色卡片（`L1_VISUAL` 颜色映射严格对齐 taxonomy L1 标题：历史→amber、哲学→purple、科学→green、经济学→blue、政治理论→coral）；L2/L3 可折叠树（默认折叠，本地 `Set<string>` 保存展开态）；时间线横幅升级为品牌实色 + 双轨药丸；统计改 4 卡网格；`formatWords()` 万进制。
- **P0 三条已并入 CSS**：① 列表 stagger（`@keyframes list-in` + `animation-delay: min(calc(var(--i,0)*40ms),320ms)`，门控 `body:not([data-sys-anim='none'])` + `prefers-reduced-motion`）；② 卡片/表头/行 `:active{transform:scale(.985)}`；③ 时间线 `.is-cross` 紫轨。
- 重打包 APK（后台 task dm2ywJ）：BUILD SUCCESSFUL 57s，**6.71MB** @ apps/web/android/app/build/outputs/apk/debug/app-debug.apk。验证：`grep -c "l1-grid\|l1-icon\|list-in"` → 1（新 CSS 在包内）。
- 注：此 APK 即"84 词条版"叠加首页 V5 改造，与 build-apk-84.log 那版（6.88MB）是不同构建——V5 仅改前端样式，内容数据未变。

## D:\WorkBuddy--Knowledge 根目录清理
- 用户嫌根目录乱，要求整理无关文件。先只读扫描，等后台 gradle 写完再动，避免打断构建。
- 16 个过时构建日志（build-apk.log、build-apk-v2~v13.log、build-apk-84.log、build-apk-deliver.log）+ 桌面误放 `1.jpg` + **损坏的 `gradle-8.11.1-all.zip`（169 字节，实为 307 重定向 HTML，非真包）**。
- 用户拍板：删 15 个过时 log + 损坏 zip；`1.jpg` 也删。
- PowerShell `Add-Type -AssemblyName Microsoft.VisualBasic`（送回收站）被安全策略拦截 → 改为 `mv` 进项目内**可恢复隔离区** `.trash-2026-09-09/`（满足"可恢复"诉求，非硬删）。
- 分两批 mv：批1 10 个旧 log；批2 其余 7 个（含 zip + 1.jpg）。根目录现仅留：`.editorconfig/.gitignore/.prettierrc/PROJECT_STATUS.md/README.md/build-apk-home-v5.log/overview.md/package.json/pnpm-lock.yaml/pnpm-workspace.yaml/tsconfig.base.json` + 6 个干净子目录（apps/content/docs/node_modules/packages/scripts/.workbuddy）。17 个文件已隔离。
- `.trash-2026-09-09/` 暂作可恢复存档；待用户确认无碍后可真删。

## docs/10 全部 UI 改进项落地（V6）
用户要求"按前文列出的所有需求逐一完整实现"，于是把 `docs/10` 的 P0/P1/P2 全做了（纯 CSS/零依赖，守红线：不装依赖、离线）。

### P0
- **P0-1 自定义确认弹窗**：新建 `components/ConfirmDialog.tsx`（portal + 遮罩 + 上滑 + 危险态红按钮）；`SettingsPage` 去掉 `window.confirm`，改用「取消/确认」中文弹窗（清空历史/清除续读）；历史清空也复用。
- **P0-2 首页类目计数 bug**：`Home.tsx` 折叠树重构——L1/L2 标题改 `<Link>` 跳 `/browse/:id`（计数始终可点进精确列表，根治"有计数但展开无子项"）；无子项且有 count 的 L2 展开显示「该分类下 x 条词条直接归属」提示。
- **P0-3 看过页**：`history.ts` 的 `HistoryItem` 加 `progress?` 字段，`addToHistory` 接收进度（阅读页按 章节/总章 估算）；`HistoryPage` 重写——按「今天/昨天/本周/更早」分组（标题吸顶）、每项进度条、顶部清空按钮（自定义弹窗）。
- **P0-4 续读卡片**：`Home.tsx` 顶部读 `getLastRead()` 显示「继续阅读《标题》」+ 进度，跳 `/entry-reader/:slug`。

### P1
- **P1-5 搜索升级**：`SearchBox` 加清除按钮；`SearchPage` 加 标题/全文 切换（Segmented 风格）、关键词高亮（`highlight()` 拆 `<mark>`）、最近搜索（localStorage `pks_recentSearches`，空查询展示）。
- **P1-6 词条详情 Hero**：`EntryCoverPage` 相关词条加 `.cover-related` 横向滚动；CSS 放大 `.entry-title`、`.cover-actions` sticky 底部、`.btn-start` 满宽。
- **P1-7 设置 List 化**：`SettingsPage` 数据管理区加 `danger-zone` 红框 + 危险提示；CSS 分组标题强化、行前加品牌圆点、版本号居中。
- **P1-8 目录增强**：`EntryReaderPage` 目录当前章加「读到这里」标记；目录面板支持「左缘右滑关闭」（touch 手势）。

### P2
- **P2-9 导航重构**：`TabBar` 改为 首页/搜索/看过/我的（设置不再独立 Tab，收进「我的」）；新增 `pages/MePage.tsx`（设置/历史/关于聚合）；`types.ts`/`router.tsx`/`App.tsx` 同步加 `me` 路由。
- **P2-10/11/12**：系统亮度（`@capacitor/screen-brightness`）/状态栏透明（`@capacitor/status-bar`）/仿真翻页/横向时间轴缩放——**受「禁止安装依赖」硬红线约束，未安装插件**；亮度用既有 CSS 叠加（brightnessVeil/filter）等价替代，状态栏已由阅读页 `PKS.setStatusBarPermanent` 隐藏实现沉浸；仿真翻页/横向时间轴标注为暂缓（P2 噱头、ROI 低）。

### 校验与构建
- typecheck 通过（`tsc -p apps/web/tsconfig`）。
- 全部新样式追加在 `styles.css` 末尾「V6 UI 改进」区块（单文件 append，无冲突）。
- 重建 APK：copy:content → vite build → cap sync android → gradlew assembleDebug（构建中/已完成）。

## 新界面截图 + 外部 UI 建议可行性评审（用户要求只分析、不敲代码）
- 用户提供 11 张真机截图 + `C:/Users/Yu/Desktop/123/知识App_UI改进建议.md`，要求评估可行性并产出报告。
- 先读了外部建议文档，发现同样基于错误技术栈（MUI6/Tailwind3/Framer Motion/React Router），与仓库实际栈不符；但同时从截图里发现了真实 bug 和高价值交互点。
- 重点分析了**首页类目计数 bug**：`buildCategoryViews` 的 `total = 自身 entrySlugs + 子节点 count 求和` 导致「有词条但无子节点的类目」展开后为空却显示数字；L1 卡片数字也可能大于可见子类数字之和（因为 L1/L2 自身可能直接挂词条）。
- 还定位了 3 个截图级问题：`window.confirm` 弹窗出现英文 CANCEL/OK；「看过」页是扁平列表缺分组/进度；设置页数据管理区缺危险视觉。
- 产出报告：`docs/10-新界面与UI建议可行性评审.md`。给出了 P0（自定义弹窗/修复计数 bug/看过分组/首页续读卡片）、P1（搜索/词条详情 Hero/设置 List 化/目录拖拽）、P2（导航重构/系统亮度/状态栏透明/仿真翻页）的分级建议，并列出外部文档中应直接忽略的部分。
- 本次仅产出报告，**未动代码**；待用户拍板后再进入实现。
