# 2026-09-15 · 分类计数模型修复 + 重建 APK

## 问题
用户反馈：App 里「哲学/形而上学」只显示 1 条，而我报告有 10 条。
根因：App 用的是 `AppContext.tsx` 的「主键分区」模型（每条只归层级最深的类目），
哲学分类有两条交叉轴——主题轴（形而上学/美学/认识论/伦理学/政治哲学/逻辑，level 2）
与时期轴（西方哲学/*，level 3）——时期轴永远压过主题轴，导致主题轴被掏空
（美学 0、形而上学 1、伦理学 1、政治哲学 1、逻辑 4）。

## 修复（方案 A：标签语义/并集去重）
- `apps/web/src/state/AppContext.tsx`：删除 `buildSlugPrimary/buildNodePrimarySlugs/buildSubtreeSlugs`，
  改为 `buildUnionSlugs`（类目计数 = 该类目含子孙所引用的全部词条 slug 去重），
  新增 `flattenNodes` 供 `nodeOwnSlugs`。`nodeSlugs` 现为并集。
- `BrowsePage.tsx` 注释同步。
- 代价：父级计数可能 < 直接子级之和（跨轴重叠）。哲学 72、历史 30、科学 30、经济学 22、
  政治理论 29、技术 8。哲学各叶：思想史 9 / 中国哲学 11 / 西方哲学 44（古希腊12 中世纪11 近代欧陆13 英美8）
  / 形而上学 10 / 认识论 7 / 伦理学 8 / 美学 11 / 政治哲学 4 / 逻辑与批判性思维 12。
- 自检：tsc --noEmit 0 error；`_selfcheck.cjs` 复算 14 项全通过。

## 重建 APK（v1.3 / versionCode 4）
- `build.gradle` versionCode 3→4、versionName 1.2→1.3（触发 WebView 缓存清理）。
- `cd apps/web && CODEBUDDY_SAFE_DELETE_ENABLED=0 node node_modules/vite/bin/vite.js build` → 400 模块，dist 新 hash（index-BQjic5J9.js 等）。
- `_sync.cjs` 整体替换 `android/app/src/main/assets/public`（1229 文件；注意 dist 不含 capacitor.config.json，本地加载 assets 不需它）。用后删。
- `cd apps/web/android && ./gradlew assembleDebug --no-daemon`（JAVA_HOME=D:\JDK\jdk-19.0.1，ANDROID_HOME=D:\Android SDK）→ BUILD SUCCESSFUL。
- APK：`apps/web/android/app/build/outputs/apk/debug/app-debug.apk` 12.1MB。
- 验证：android assets/public 的 taxonomy.json 正常（48 节点含 entrySlugs），新逻辑复算 美学=11 / 形而上学=10。

## 提交
- （待提交）fix(web): 分类计数改标签语义 + versionCode 4。


承接 2026-09-14：新增 28 条哲学词条（132→160）后重建 APK。用户指令「重建 APK」。

## 完整链路（本环境无 Capacitor CLI，cap sync 不可用，须手工）
1. **升版本**（触发 WebView 缓存清理）：`apps/web/android/app/build.gradle` `versionCode 2→3`、`versionName 1.1→1.2`。
2. **重建索引**：`node node_modules/.pnpm/tsx@4.23.13/node_modules/tsx/dist/cli.mjs packages/cli/src/index.ts build:index`
   → 160 词条 / 769 章 / 129.7 万字 / 16 检索分区 / 445089 词项。
3. **搬运到 web**：`node scripts/copy-content.mjs` → `apps/web/public/content`（entry 分片 24→25，新增 `w` 片）。
4. **vite 构建**：`CODEBUDDY_SAFE_DELETE_ENABLED=0 node node_modules/.pnpm/vite@5.4.21_@types+node@22.20.1/node_modules/vite/bin/vite.js build apps/web`（≈48s，400 模块）。
5. **手工投递到 Android assets**：写 `SYNC.cjs`，`rm -rf` + 递归拷贝 `apps/web/dist` → `apps/web/android/app/src/main/assets/public`，并写出运行时 `capacitor.config.json`（appId com.pks.app / appName 知识 / webDir dist / server.androidScheme https / android.allowMixedContent true）。本次 1230 个文件（上次 1033）。**用后删 SYNC.cjs**。
6. **打包**：`build-apk.bat`（自含 `cd /d` 到 android 目录 + JAVA_HOME/ANDROID_HOME + `gradlew.bat assembleDebug`），用后删。≈1m10s。

## 核验（务必解压，不能靠字符串搜索）
APK 是 zip：**文件名可见但内容被 deflate 压缩**，所以搜索 `"s":"laozi"` 必然 false，必须解压分片再解析。
自写 zip 解析（EOCD→central dir→local header→`zlib.inflateRawSync`）：
- versionCode 3 / versionName 1.2；12.06 MB；1703 个 zip 条目。
- catalog 25 分片 / 160 items / 160 唯一 slug；28 条新词条的目录项与 `content/entries/<slug>/*.md` 正文**全部在包内**。
- manifest.stats：`{categories:58, entries:160, sections:769, words:1297423, bytes:3892269, shards:16}`。

## 提交
- `9edbdab` content(philosophy)：28 条新词条（168 文件 / +14594 行），已推 gitee。
- `0af070c` build(android)：versionCode 2→3，已推 gitee（`9edbdab..0af070c`）。

## 复用要点
- 内容变更后「重建 APK」= 升版本 → build:index → copy-content → vite build → SYNC.cjs → gradle assembleDebug → 解压核验。
- 每升一次 versionCode，设备的 WebView 缓存清理逻辑就会触发一次，避免旧内容残留。

## 修复：`npm run dev` 报「'vite' 不是内部或外部命令」
- 根因：`apps/web/node_modules/.bin/` **整个目录缺失**（`packages/cli` 同样，但其无 binnable 依赖，属正常）。
  09-14 误删 `apps/web/node_modules` 后，`scripts/tools/recover-nm.mjs` 只重链了顶层包，**漏建 `.bin` 启动器**。
  之前几天的会话一直用 `node node_modules/.pnpm/<pkg>/...` 直调底层入口，所以掩盖了该问题；`npm run dev` 依赖 `.bin/vite` 才暴露。
- 修复：从 `packages/core/node_modules/.bin/` 复制同款 `vite` / `vite.cmd` / `vite.ps1` 到 `apps/web/node_modules/.bin/`
  （两级 `.bin` 到仓库根深度一致，pnpm shim 内相对路径通用）。验证：dev server HTTP 200。
- 已补强 `scripts/tools/recover-nm.mjs`：新增 `copyBin('vite')` + 缺失包告警。
- ⚠️ `@capacitor/cli` / `@capacitor/core` 在 `.pnpm` store 中**已彻底不存在**（非仅软链缺失），手工无法恢复 → `cap sync` / `npm run android:build` 仍不可用；继续按上文「手工 SYNC.cjs」流程打包。若想恢复 cap，需联网重装 @capacitor/* （会触及 node_modules，先确认）。
- 另注：`apps/web/package-lock.json` 被提交入库（npm lockfile），是误操作残留；项目用 pnpm，建议删除，勿再于 apps/web 执行 `npm install`。
