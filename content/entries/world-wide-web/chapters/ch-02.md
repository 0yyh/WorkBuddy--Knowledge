---
schema: 1
slug: world-wide-web/ch-02
work: world-wide-web
key: ch-02
title: 第二章 三大基石
order: [2]
path: [第二章 三大基石]
depth: 1
status: published
license: CC-BY-SA-4.0
ai_generated: true
ai_annotated: true
sources:
  - title: "HTTP - MDN Web Docs"
    url: https://developer.mozilla.org/
    license: Fair-Use
summary:
  tldr: URL 定位、HTTP 传输、HTML 呈现，三基石使链接成为全球知识的组织方式。
  keyPoints:
    - URL 给每个资源一个全球唯一、可书写的地址。
    - HTTP 是无状态的请求—响应协议，简单而可扩展。
    - HTML 用标记描述文档结构与链接，浏览器负责渲染。
    - 三者皆开放标准，任何人都可实现客户端与服务器。
---

万维网的力量来自三项技术的**极简组合**。每一项都朴素到几乎平凡，但组合起来，重写了人类知识的组织方式。

**URL（统一资源定位符）**解决「如何指认」。一个 URL 由协议名、主机名与路径构成（如 http://www.example.com/page），全球唯一、可书写、可引用。它的革命性在于：任何文档都可以在正文里写下另一份文档的**完整地址**，读者点击即达——知识的引用从脚注与书单，变成了「一键跳转」。URL 也使 Web 成为一个**可书写的空间**：你可以在自己的页面上链接任何人的任何页面，无须许可。

**HTTP（超文本传输协议）**解决「如何传输」。它是一个极简的**请求—响应**协议：浏览器发一个「GET /page」的请求，服务器返回文档内容。HTTP 刻意设计为**无状态**——服务器不记住上一个请求是谁发的，这使服务器可以为海量并发服务，也使协议简单到任何设备都能实现。它的可扩展性同样关键：头部字段可以携带缓存、认证、内容类型等任意信息，为后来的cookies、API 调用留足了空间（详见「HTTP」词条）。

**HTML（超文本标记语言）**解决「如何呈现」。它用标签描述文档的**结构**——标题、段落、列表、图像，以及最重要的：`<a href="...">` 链接。HTML 刻意不规定「长什么样」，而只规定「是什么」；样式交给浏览器渲染。这一「结构与样式分离」的思想，使同一份文档可以在任何设备上合理显示，也为后续 CSS 的出现留下接口。

> 📜 「Web 的力量在于它的普适性——任何人都能访问，无论何种设备、何种能力。」 —— 蒂姆·伯纳斯-李对 Web 设计原则的表述

三项基石共同的制度属性，比技术属性更重要：它们都是**开放标准**，由 W3C 与 IETF 等组织以公开流程维护，任何人无须授权即可实现自己的浏览器或服务器。正是这种「可自由实现」保证了 Web 的中立与繁荣——它不属于任何公司，因此人人都愿意在上面建设。

> 📝 编者注：URL 的另一个名字 URI（统一资源标识符）更严格，URL 是其子集。日常语境两者常混用。

## 一次典型请求的完整旅程

把三大基石串起来看，一次网页访问是这样的：用户在地址栏输入 URL，浏览器先做域名解析，把主机名换成 IP 地址；随后按 HTTP 约定发起请求，服务器返回响应（状态码、头部与正文）；浏览器解析返回的 HTML，遇到图片、样式表与脚本再发起新的请求，同时依据头部字段决定是否使用本地缓存。整个过程是**无状态、可并行、可缓存**的——没有会话记忆，意味着服务器可以用同一套逻辑服务海量用户；可以缓存，意味着同一份内容不必反复长途搬运。

这套结构也解释了 Web 的可扩展性从何而来：新内容类型通过头部字段协商，新功能通过新增方法与头部实现，旧的客户端即便不认识新字段，也往往能优雅降级。协议因此得以在上亿台异构设备上长期共存。

## 无状态的双刃与补课

无状态既是优点也是负担。服务器不记得上一个请求是谁发的，那么「登录状态」如何维持？答案是 **Cookie 与令牌**：服务器在响应里下发一小段凭据，客户端此后每次请求自动携带，服务器据此重建会话。这一补丁带来了登录、购物车与个性化，也带来了跟踪与隐私问题——第三方 Cookie 因此成为隐私监管的重点对象。

类似的「在极简协议上堆叠复杂度」的模式贯穿 Web 史：缓存、压缩、安全传输、身份认证、内容协商，都是通过新增头部或新增协议层实现的。这提示了一条设计经验：**核心保持简单，复杂性放在边缘**，是系统能够长期演化的常见前提。

## 开放标准的治理意义

URL、HTTP 与 HTML 都不是某家公司的私有技术，而是由万维网联盟与互联网工程任务组以公开流程维护的标准。任何人无需授权即可实现浏览器或服务器，这一点看似只是技术便利，实则是**竞争结构**的前提：正因为没有独占权，浏览器才可能多家竞争，Web 才不会被单一厂商的扩展方向绑死。

标准治理的代价是决策缓慢与妥协繁多，收益是长期稳定与广泛采纳。后来的每一次「开放协议对封闭平台」之争，本质上都是在重新衡量这笔账——这构成了下两章的主线。

<!-- PKS_EXPANDED_V5 -->
